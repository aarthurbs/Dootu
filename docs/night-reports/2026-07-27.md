# Relatório noturno — 2026-07-27 (Fluxos → n8n, Fase 2)

## ⚠️ Nota de setup para o humano revisar (LEIA PRIMEIRO)
- O agente foi invocado **na branch `main`**, não numa `night-auto/<data>` como o prompt do agente assumia.
- Conflito de instruções: o prompt manda usar branch dedicada + commit por item; **o backlog `plans/fluxos-n8n-backlog.md` (decisão do usuário 2026-07-27, confirmada na memória `projeto-fluxos-n8n`) manda o oposto: "Trabalho direto no `main`, sem commit — o usuário revê o diff acumulado".**
- Resolvi a favor da instrução **mais específica e recente** (o backlog): **trabalho no working tree, SEM commit**, deixando o diff acumular para revisão. Isso NÃO altera o histórico e respeita "sem push / sem merge".
- Reversão de mudança quebrada = `git checkout -- <arquivo>` / desfazer via Edit (funciona igual em working tree não-commitado).

### Estado git da 2ª execução (18:12+) — ⚠️ limpeza p/ o humano de manhã
- Esta execução também começou na `main`. Criei `night-auto/2026-07-27` (rule #2 do prompt), depois decidi seguir o modelo do backlog (sem commit) e tentei voltar pra `main` — **um `.git/index.lock` obsoleto bloqueou `git checkout`** e eu **não consegui remover o lock** (arquivo sensível → permissão negada no modo headless).
- Resultado: estou **parada na branch `night-auto/2026-07-27`, que aponta pro MESMO commit `2fd798d` da `main` (zero commits novos)**. Como não commitei, o diff do working tree é **idêntico** ao que a `main` veria — a review de manhã não muda.
- **Ações p/ o humano de manhã:** (1) apagar `.git/index.lock`; (2) `git checkout main` (traz o working tree junto) e `git branch -d night-auto/2026-07-27` (branch vazia); (3) revisar `git diff` + arquivos novos (`git status`) — a fonte da verdade é o working tree, como o `plans/README.md` avisa.

## Concluído
- **[x] Item 1 — Importar / Exportar quadro como JSON** (sem commit, diff no working tree).
  - Toolbar: **Exportar** (download `.json`), **Importar** (upload via `<input type=file>` + FileReader) e **Colar** (`prompt()`). Reusam `.fx-btn` → **zero CSS novo**.
  - `boardFromJSON(text)` PURO/testável: valida `nodes`/`edges`, normaliza, descarta arestas órfãs, gera id de board novo. Aceita `{board:{…}}` ou o board cru.

- **[x] Item 2 — Duplicar nó (Ctrl+D ou botão) + Delete p/ excluir** (sem commit).
  - Botão **⧉** no header (`data-act="node-dup"`) + CSS `.fx-node-dup` (hover neutro, não-destrutivo).
  - `duplicateNode(board,id)` PURO (id novo, +26/+26, copia type/title/body/color/lang).
  - Teclado: **Ctrl/Cmd+D** duplica o nó focado; **Delete** exclui só quando o CARD está focado (nunca em input/textarea). Card ganhou `tabIndex=-1`.

- **[x] Item 3 — Decisão com 2 saídas (V/F): o código retorna boolean e a execução segue o ramo certo** (sem commit).
  - **Modelo (puro, testável):** `addEdgeTo(board,from,to,branch)` grava `edge.branch = true|false` (ausente = aresta incondicional, retrocompatível — o seed continua 5 nós/4 arestas). Novas funções puras `edgeTaken(board,e,decisions)` e `activeNodes(board,decisions)` decidem quais nós ficam ATIVOS dado o booleano de cada Decisão (o ramo errado é podado). `boardFromJSON` preserva `branch`. Exportadas em `module.exports`.
  - **Execução (`runFlow`):** a Decisão agora **executa o corpo** no mesmo Web Worker sandbox (só se tiver ≥1 saída rotulada V/F — senão é pass-through, o que preserva o seed que tem body em pseudocódigo); o resultado vira booleano (`!!out`) e só **roteia** (os dados de entrada seguem inalterados). Nós no ramo não seguido ficam com estado **`skip`** ("⊘ ramo não tomado") em vez de rodar (BP-008: estado visível mesmo quando NÃO age). Erro na Decisão → nenhum ramo rotulado é seguido.
  - **UI (skill `emil-design-eng` invocada antes):** 2ª porta de saída `.fx-port-false` (topo 40px, borda `--danger`); a porta de cima da Decisão vira VERDADEIRO (borda `--success`); `title` descritivo em cada porta; geometria do fio vem do MODELO (`outPtFor(n,branch)` usa `PORT_Y`/`PORT_Y_F`). Cores são **funcionais** (não glow gratuito). Estado skip = `opacity:.5` + `opacity var(--ease)` na transição base de `.fx-node`. Sem dependência nova, sem backend.
  - Criação do fio: arrastar da porta de baixo (`.fx-port-false`) → `branch=false`; da porta de cima numa Decisão → `branch=true`; de qualquer outro nó → incondicional.

- **[x] Item 4 — Painel lateral de config do nó (edição maior, sem apertar o card)** (sem commit).
  - Botão **⤢** no header do nó (`data-act="node-edit"`, ao lado de ⧉/✕, excluído do drag) abre um **painel lateral** (`aside.fx-panel`) com Título, Cor, Linguagem (só p/ nós Código) e um **Conteúdo** grande (`textarea` que cresce `flex:1`, min 200px). ✕ ou **Esc** fecha.
  - **Painel PERSISTENTE:** criado 1x (`panelNode()`), re-anexado ao `.fx-stage` a cada `render()` — identidade estável dá transições de abrir/fechar limpas (sem re-criar o nó → sem re-disparar animação a cada render). `syncPanel()` popula do modelo e alterna `.fx-panel--open` (chamado no fim de `render()` e ao abrir/fechar).
  - **Sync sem re-render (não perde foco):** editar no painel escreve no modelo e **espelha no card** (`setCardVal`/`--nc`); editar no card, com painel aberto no mesmo nó, chama `syncPanel()` de volta. Lang do Código também espelha `.fx-node-lang`.
  - **UI (framework Emil aplicado):** drawer `translateX(100%)→0` + opacity, `var(--dur-2)`(220ms) `var(--spring-soft)` (ease-out forte, sem overshoot); só transform/opacity (GPU); nunca `scale(0)`; `@media (prefers-reduced-motion)` deixa só o fade (acessibilidade). `.fx-stage` ganhou `overflow:hidden`+`border-radius` p/ o painel fechado (fora à direita) não criar scroll horizontal. Sem dependência nova. Sem lógica pura nova → sem assert novo (coberto pelo smoke DOM que agora exercita `panelNode`/`syncPanel`).

## Testes rodados
- Baseline: `node --check fluxos.js && node test-fluxos.js && node test-fluxos-exec.js && node test-fluxos-dom.js` → **verde**.
- Após item 1 (+5 asserts `boardFromJSON`): **verde** (18:04).
- Após item 2 (+5 asserts `duplicateNode`): **verde** (18:07).
- Após item 3 (+11 asserts: `addEdgeTo` branch, `edgeTaken`, `activeNodes` poda V/F/null, `boardFromJSON` preserva branch): **verde** (18:23) — os 4 checks passaram.
- Após item 4 (UI pura, sem assert novo; smoke DOM agora passa por `panelNode`/`syncPanel`): **verde** (18:33) — os 4 checks passaram.

## Arquivos alterados (working tree, sem commit)
- `fluxos.js` — itens 1–4. Item 3: `addEdgeTo` (branch), `edgeTaken`/`activeNodes` (novas, puras), `outPtFor`, `nodeEl` (porta falsa da Decisão), `onPointerDown`/`onPointerMove`/`onPointerUp` (fio com branch), `markNodeState` (estado skip), `runFlow` (executa Decisão + poda de ramo), `boardFromJSON` (preserva branch), `module.exports`. Item 4: vars `panelEl`/`editingId`, `cardEl`/`setCardVal`/`panelNode`/`syncPanel`, `render` (fx-stage + append painel + syncPanel), `nodeEl` (botão ⤢), drag guard, `onClick` (node-edit/panel-close), `onInput` (campos do painel + espelho), `onChange` (lang do painel), `onKey` (Esc).
- `index.html` — CSS: item 2 (`.fx-node-dup`); item 3 (`.fx-port-false`, `.fx-node--decisao .fx-port-out`, `.fx-run-skip`, `.fx-node-result--skip`, `opacity` na transição de `.fx-node`); item 4 (`.fx-node-edit`, `.fx-stage`, `.fx-panel*`, reduced-motion).
- `test-fluxos-exec.js` — +5 (item 1), +5 (item 2), +11 (item 3).
- `plans/fluxos-n8n-backlog.md` — itens 1–4 → [x].

## Pendências
- Itens 5–8 da Fase 2 (próximos): minimapa/fit-view, múltipla seleção (box-select) + arrasto em grupo, undo/redo, notas/sticky + frames de agrupamento.

## Possíveis problemas / p/ o humano revisar de manhã
- **Git:** ver "Estado git da 2ª execução" acima — apagar `.git/index.lock` e a branch vazia `night-auto/2026-07-27`.
- **Item 3 — regressão do seed evitada de propósito:** a Decisão do seed (`test_port aberto?`) tem corpo em pseudocódigo (não-JS) e **só uma saída sem rótulo** → cai no caminho pass-through (não executa), então NÃO vira ✗ erro no ▶ Executar. A execução real da Decisão só liga quando o usuário conecta uma saída V ou F. Confirmar que é o comportamento desejado.
- **Item 3 — smoke DOM não exercita `runFlow`** (não há trigger de ▶ no headless). A lógica de branch/poda é coberta pelos asserts puros de `activeNodes`/`edgeTaken`, mas o caminho DOM real (portas, skip visual) precisa de teste manual no navegador via `http://localhost` (Worker não roda em `file://`).

---
Orquestrador: 24 execucao(oes), branch night-auto/2026-07-27, encerrado 2026-07-27 18:44. Log em C:\Users\Teste\AppData\Local\Temp\night-agent-2026-07-27.log


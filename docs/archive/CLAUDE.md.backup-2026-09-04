# Contexto do Projeto
Plataforma client-side do vendedor (Vanilla JS, `index.html`, dados em `localStorage`): Prompts, Planilha de Faturador (SKU), Radar, Painel do Empreendedor, Fluxos e Estúdio de Vídeos, mais widgets pessoais (placar NBA/Copa ao vivo). Módulos REMOVIDOS: **Amazon FBA** (2026-06-29), e **Precificação**, **Inventário Amazon** e **Shooting Range** (2026-08-17) - ver Domínio do Negócio. O backend full-stack (Supabase/SP-API) está planejado, mas PAUSADO.

## Arquitetura alvo (ver ARQUITETURA.md)
- Frontend: site Vanilla JS atual (`index.html`) — NÃO reescrever; evoluir incrementalmente.
- Backend: Supabase (Postgres + Auth + Edge Functions + RLS).
- Integração: Amazon SP-API, acessada SOMENTE por Edge Functions (Deno). O navegador nunca fala com a Amazon.

## Regra de segurança inviolável
- Segredos SP-API/LWA (client_secret, refresh_token) e a `service_role` key NUNCA vão para o frontend nem para o banco acessível ao cliente. Só em env das Edge Functions / Supabase Vault.
- Toda tabela com RLS escopada por `user_id = auth.uid()`. `amazon_credentials` não tem policy de cliente (só service_role).

## Transição (importante)
- O módulo legado ainda usa `localStorage` (`pp_prompts_v1`, `pp_projects_v1`). Migração para Postgres é incremental e não pode quebrar o que já funciona.
- Não criar servidores Node próprios nem dependências npm no frontend; o backend é o Supabase.

# Domínio do Negócio (Multi-marketplace + Engenharia de Prompts)
- **Precificação REMOVIDA (decisão do usuário, 2026-08-17):** a aba inteira saiu - `window.Calc5` (comparador de Margem de Contribuição em Mercado Livre Clássico/Premium, TikTok Shop, Amazon e Shopee), a seção `#view-precificacao`, os blocos `<style id="calc5-styles">` e `<style id="prc-options-styles">`, os scripts `calc5-script` e `prc-options-toggle-script`, o item do menu, o card da Central e `#count-pricing`. Não recriar nem referenciar `Calc5`/`RATES`/`prc*`/`#prc-*`/`.plat-*`/`.pdv-*`. O modelo ANTERIOR (margem-alvo / "Bater concorrente" / `pricing_v1`) já havia sido apagado em 2026-06-26 - também não recriar.
- **Amazon FBA removido (2026-06-29):** a **Central Amazon FBA** (publicação de anúncio — caminhos `buybox`/`zero`, `amz_products_v1`) foi apagada (menu, seção, CSS, JS, arquivos). Não recriar nem referenciar `setupAmazon`/`view-amazon`/`amz_products_v1`.
- **Inventário Amazon REMOVIDO (decisão do usuário, 2026-08-17):** saiu `amazon-inventory.js`, `test-ponte.js`, a seção `#view-amazon-inventory`, o item do menu, o card da Central e o snapshot `pp_amazon_inventory_v1`. **Saíram junto os recursos do Faturador que dependiam dele:** `fatInvData`, `fatMissingSkus`, `fatStagedSkus`, `fatRenderInvNotice`, `fatWeeklyDue`, `fatRenderWeekly`, `fatRefreshInvBadge`, o aviso `#fat-inv-notice`, o lembrete de terça e o auto-preenchimento `pp_fat_autofill_v1`. **`fatAddGroupWithSkus` FICOU** - tem outro chamador (agrupamento por SKU base). **Atenção ao CSS:** `.amz-inv-head` e `.amz-inv-kicker` foram PRESERVADAS porque o **Radar** as reaproveita; o resto do CSS `.amz-*` saiu. Isso reverte a decisão de 2026-06-30 ("ATIVO, MANTER") — não recriar.
- **Radar (E-commerce + IA) — FUNDIDOS numa aba só (decisão do usuário, 2026-07-14):** antes eram duas abas (`ecom-news` + `ia-news`); agora é **uma** nav `data-view="radar"` / `#view-radar` com um **seletor segmentado** (`.radar-seg`, botões `data-radar="ecom|ia"`) que alterna os painéis `#radar-panel-ecom` / `#radar-panel-ia` — **as infos NUNCA se misturam**, o usuário troca de feed. **Dados, agentes e renderers ficaram intactos:** `ecommerce-news-data.js`/`window.ECOM_NEWS` (sábado) e `ai-news-data.js`/`window.AI_NEWS` (diário à meia-noite); renderers `ecommerce-news.js`/`ai-news.js` continuam escrevendo nos MESMOS IDs `#ecom-*`/`#ia-*` (por isso não foram tocados). Nav badge = `#count-radar`, setado por um script inline curto no rodapé do `index.html` (conta os `.ecom-card` do feed ativo, roda após os renderers). `#count-ecom`/`#count-ai` deixaram de existir — os renderers têm guarda `if(count)`, então viram no-op. Atualização SEM runtime segue igual: tarefas do Agendador do Windows rodam `radar-ecommerce.ps1` (sáb) e `radar-ia.ps1` (diário) → `claude.exe -p` headless. Site 100% estático (sem fetch/IA em runtime) — exceção de conteúdo externo autorizada (junto do placar ESPN).
- Prompts estruturados seguem os 5 pilares: 1. Papel, 2. Contexto, 3. Tarefa, 4. Formato, 5. Restrição.

- **Radar Claude & Loops — INTEGRADO (decisão do usuário, 2026-07-21):** terceiro feed separado dentro de `data-view="radar"`, acionado por `data-radar="claude"` e exibido em `#radar-panel-claude`. Dados estáticos em `claude-radar-data.js` (`window.CLAUDE_RADAR`) e renderer em `claude-radar.js`; contém novidades oficiais da Claude/Anthropic, skills e loops aplicáveis e estratégia experimental de mercado. Atualizações programadas: Claude/skills às 00:00 e 18:00; mercado às 04:00. Validar com `node --check claude-radar-data.js`, `node --check claude-radar.js` e `node test-claude-radar.js`. Não misturar esse conteúdo com os painéis E-commerce ou IA.

- **Central (home) + categorias no header — IMPLEMENTADO (decisão do usuário, 2026-07-24):** implementa a decisão antiga de separar "Amazon & Planilhas" vs "Empreender", na variante "os dois juntos". (1) **Barra segmentada no header** `.world-seg` (3 botões `data-world="central|amazon|prog"`) — filtra os `.nav-item[data-group]` da sidebar E os `.hub-block` da Central por mundo. (2) **Tela Central** = home do site: `data-view="central"` / `#view-central`, 2 blocos de cards (`.hub-card[data-view]`) que abrem cada ferramenta. Grupos: *amazon* = faturador (precificacao e amazon-inventory saíram em 2026-08-17); *prog* = radar/**empreendedor**/**fluxos**. JS no script inline: `const VIEW_GROUP` (mundo de cada view) + `function setWorld()`; `'central'`/`'fluxos'` entraram no `toolViews` de `activateView`; INIT abre em `activateView('central')`. `data-group` na sidebar ≠ `[data-cat]` (categorias de prompt) — não colidem. CSS `.hub-*`/`.world-seg*` inline. Sem lib, sem IA.
- **Painel do Empreendedor — controle de processos do negócio, ATIVO (decisão do usuário, 2026-07-31):** aba `data-view="empreendedor"` / seção `#view-empreendedor`, mount `#emp-root`, renderer `empreendedor.js` (Vanilla JS estático, `localStorage` `pp_empreendedor_v1` — sem IA, sem backend). Semeado com **6 trilhos** (processos): Identidade da marca, Radar de mercado, Laboratório de conteúdo, Rede de perfis (satélites), Funil de clientes e Lucro real. Cada trilho tem **etapas (checklist)**; o `status` (a fazer / fazendo / feito) e o `progresso %` são **derivados** das etapas (guarda-zero, BP-004). Cabeçalho **"Meta 90 dias"** (dias decorridos/restantes + contador Clientes 0/10) e um `log` (histórico) curto por trilho. Adicionar/editar/remover trilho e etapa via `prompt()` + botões explícitos ✎/✕ (nunca dblclick — BP-001). CSS `.emp-*` inline no `index.html`. Vive no mundo **Empreender** ao lado de **Fluxos** e **Radar**. Checks: `node --check empreendedor.js` + `node test-empreendedor.js`.
- **Fluxos — construtor de mapas de processo estilo n8n, ATIVO (decisão do usuário, 2026-07-24):** aba `data-view="fluxos"` / `#view-fluxos`, mount `#fluxos-root`, renderer `fluxos.js` (script no `index.html`). Canvas com nós arrastáveis (pointer events), portas in/out ligadas por conectores bézier (SVG), 4 tipos (`inicio/processo/decisao/codigo`), vários **quadros** ("boards") e tudo em `localStorage` `pp_fluxos_v1`; semeado com um fluxo-exemplo do `scanner.py`. Geometria das conexões vem do MODELO (`node.x/y` + offsets fixos `NODE_W`/`PORT_Y`), não de `getBoundingClientRect` — desenha certo mesmo com a aba escondida e no arraste. Edição inline sem re-render (preserva foco); delete de nó/conexão por botão ✕ explícito (BP-001); `prompt()`/`confirm()` p/ boards. CSS `.fx-*` inline. **NÃO tratar como código morto.**
  - **Navegar/zoom (2026-07-27):** Ctrl + botão direito arrasta o canvas (via scroll do `.fx-canvas-wrap`); Ctrl+scroll dá zoom no ponto do cursor; botões `− 100% +` na toolbar. Zoom = `transform: scale(zoom)` no `.fx-canvas` (origin 0 0) + um `.fx-sizer` que faz o scroll acompanhar. `toCanvas()` divide por `zoom`; `CANVAS_W/H`=2600/1700 devem casar com o CSS `.fx-canvas`/`.fx-sizer`.
  - **Personalização por card (2026-07-27):** swatch de cor nativo por nó (`--nc` salvo em `n.color`) e, nos nós `codigo`, um `<select>` de linguagem (`n.lang`, só rótulo). `spellcheck=false` em título/corpo (matava a percepção de "erro" pelos sublinhados vermelhos) + estado `:focus-within` no card.
  - **Execução client-side "meio-termo n8n" (decisão do usuário, 2026-07-27):** a regra antiga "NÃO executa nada" foi **flexibilizada**: o botão **▶ Executar** roda o fluxo em ordem topológica (`execOrder`) e executa os nós **`Código`** (JS) num **Web Worker sandbox** (`WORKER_SRC`, timeout 3s mata loop infinito, `fetch`/`XHR`/`WebSocket`/`importScripts` bloqueados — **sem rede, sem backend, sem npm**). Nós não-código são pass-through; `Início` semeia dados se o corpo for JSON. Resultado por nó em `.fx-node-result` (✓/✗/→). **Precisa de `http://localhost` — `new Worker(blob:)` falha em `file://` (mostra aviso).** Check: `node --check fluxos.js` + `node test-fluxos.js` + `node test-fluxos-exec.js`.
  - **Atividades + "Código final" (decisão do usuário, 2026-07-28):** cards de `Código` podem pertencer a uma **Atividade** (agrupamento leve) — `board.activities=[{id,name}]` (ordem do array = ordem explícita) + `node.activity` opcional. Badge `.fx-node-act` no card; `<select>` de atividade no painel de edição (com "➕ Nova atividade…"); criar/renomear/ordenar (↑/↓) no painel **Código final**. O botão **`</> Código final`** abre um painel-overlay à DIREITA (`.fx-code`, z-index 5 abaixo do painel de edição, NÃO empurra o canvas) que compõe o código de cada atividade **na ordem das CONEXÕES** (não da posição) — funções puras `composeCode`/`activityOrder` (atravessam nós intermediários; card único vale). Trechos juntados por linha em branco, texto/indentação preservados (`esc()` + `<pre>`); copiar por bloco com feedback "Copiado ✓"; recolher por atividade; ordem ambígua (soltos/ramificação) → aviso "Conecte os cards…" (não escolhe ordem calada); linguagens diferentes → blocos separados (não mistura). Atualiza sozinho ao editar/conectar/desconectar/excluir, preservando o próprio scroll. Cards antigos sem atividade caem em "Sem atividade". Persistido em `pp_fluxos_v1`; `boardFromJSON`/`exportBoard` preservam atividades. **BUG-010 (canvas saltava ao excluir) corrigido com `renderKeepingScroll()` → BP-013.** Check: `node --check fluxos.js` + `node test-fluxos.js`/`test-fluxos-dom.js`/`test-fluxos-exec.js`.

- **Estúdio de Vídeos — CINCO telas e nada além (decisão do usuário, 2026-08-21; substitui o mapeamento de 2026-08-19):** `data-view="video-ops"` / `#view-video-ops`, mount `#video-ops-root`, renderer `video-ops.js`, CSS externo `video-ops.css`. A barra `.vop-flow` tem o fluxo `1 Vídeo` (`data-tab="overview"`) › `2 Cortes` (`cuts`) › `3 Clips` (`review`) e, ao lado em escala menor, **`Central`** (`central`) e **`YouTube`** (`youtube`). `FLOW_HINT` diz o que fazer na tela atual (cinco frases, uma por tela).
  - **O pipeline de publicação foi APAGADO (decisão do usuário, 2026-08-21):** saíram as telas `Material` (`sources`), `Posts` (`queue`), `Direitos` (`creators`), `Contas` (`accounts`) e `Relatórios` (`reports`) **e todo o código atrás delas** — contas, linhas editoriais, criadores, autorizações, fontes, variantes/posts, aprovação por hash, publicação, métricas, caminhos do Drive, CSV, logs, backup/importação, `sanitizeState`, `migrateV3` e os diálogos modais. `video-ops.js` foi de 4.904 para ~1.500 linhas; `video-ops.css` de 1.213 para ~889 (165 regras mortas cortadas). **Não recriar nem referenciar** `STATE`/`accounts`/`variants`/`sources`/`creators`/`permissions`/`driveArtifacts`/`resolveVariant`/`approvalIssues`/`toCsv`/`drivePath`/`publicationPackage`/`buildReport`/`applyResult`/`renderJobFor`/`adoptSessionVideo`/`openPromoteDialog`. Isso reverte o "ficou inteiro e acessível ao lado" (Passo 3) e o "Nada foi removido" do `<details class="vop-more">`.
  - **A chave antiga NÃO é lida nem apagada:** `pp_video_ops_v1` (schema 4) continua intacta no navegador de quem já usava — só não tem mais tela. A persistência nova é **`pp_video_clips_v1`** (`LIB_VERSION 1`), uma lista só. Apagar dado do usuário não é papel de um refactor de tela; quem quiser o cadastro antigo o encontra lá.
  - **Central de Clips = a única coisa persistida.** `centralHTML()` mostra um cartão por MP4 já baixado, **agrupado pelo nome do vídeo de origem** (`libGroups`, mais recente primeiro). Registro: `{ id, videoName, videoUrl, clipName, inSec, outSec, fileName, savedPath, bytes, origin: 'local'|'youtube', createdAt }` — `libEntry` recusa intervalo impossível e arquivo sem nome. Cada cartão tem **prévia do próprio corte** (`/clips/<arquivo>`), intervalo, tamanho, data, **⬇ Baixar vídeo** (âncora nativa `download`, sem fetch/blob) e "Remover do histórico" (tira o registro, **não** o arquivo — o `confirm()` diz isso). Sem `savedPath` o cartão explica que o clip foi só para Downloads em vez de oferecer link morto (BP-008). `libAdd()` é o **ponto único** de registro, chamado pelos dois caminhos de download (corte local e render do Remotion) — nenhum deles pode esquecer. O cartão reaproveita `.vop-entity-card`/`.vop-entity-grid` (já existiam, já são responsivos); CSS novo é só `.vop-lib-*` e `.vop-yt-rights`.
  - **Tela YouTube = a porta de entrada por URL**, agora sem cadastro: `YT` é estado de SESSÃO (`url`, `videoId`, `candidates`, `authorized`, `preview`) e morre com a aba. Cola a URL → **Detectar cortes** (`/api/yt-probe`, não baixa mídia) → cartões de trecho com sinais/nota/gancho/aviso de contexto → **Baixar trecho** (`/api/yt-fetch`) → **Editar no Remotion** / **Sem legenda** (`/api/remotion-render`), que registra o resultado na Central. Trocar a URL derruba as sugestões e a declaração do vídeo anterior (`ytUrlWrite`) — sugestão apontando para outro vídeo seria mentira.
  - O caminho antigo (subir MP4 local → `POST /api/video-cut` → FFmpeg) **continua intacto** nos passos 1-2-3; o caminho por URL é paralelo, não substitui.
  - **Descoberta sem baixar vídeo:** `video-worker/ytclip.py` (novo). `probe()` faz UMA chamada ao yt-dlp (`--dump-single-json`) mais um GET na legenda json3 — dezenas de KB para um podcast de horas, **zero byte de mídia**. `candidates()` é **pura** e cruza três sinais: o `heatmap` de "Mais reproduzidos" que o YouTube publica na página (100 baldes, dado observado — **não** é analytics privado, e quando o vídeo não publica, nenhuma sugestão cita audiência), os capítulos, e a legenda (pergunta, número, ênfase, pausa). Sinais no mesmo ponto fundem num candidato e somam nota. `cues_for_range()` recorta as falas e **zera o relógio no começo do corte**.
  - **O trecho sugerido é objeto de SESSÃO (2026-08-21):** `ytCandidateClips()` devolve `{ id, topic, inSec, outSec, hook, reason, score, contextWarning, category, signals, clipToken, clipBytes, clipCues }` em `YT.candidates` — nada é persistido e a máquina de estados `candidate → selected → editing → review → approved` do schema 4 **não existe mais** (era do pipeline). `clipToken` preenchido = trecho já em disco no cache do helper, e é o que libera o botão do Remotion.
  - **Rotas novas no `video-worker/serve.py`:** `POST /api/yt-probe` (analisa, não baixa), `POST /api/yt-fetch` (baixa **só** o trecho aprovado + devolve as legendas dele), `POST /api/remotion-render` (edita e devolve o MP4). A guarda de rota única virou despacho por dicionário, então as três herdaram o tratamento de erro que já existia.
  - **`studio/` (novo) = camada de edição.** Projeto Remotion com `package.json` próprio. **O `index.html` continua Vanilla JS sem npm** — `studio/` não é servido ao site, e a única ponte é a rota de render chamando `npx remotion render` como processo. 1080x1920, fundo do próprio vídeo borrado, vídeo centralizado, título e legenda; presets `legenda` e `limpo` **só**. `--public-dir` aponta para a pasta de cache, então **nenhum byte de vídeo é escrito dentro do repositório**. Ver `studio/README.md`.
  - **Skill `remotion-edit`** (`.claude/skills/remotion-edit/SKILL.md`): edita um corte já aprovado, aplica só o pedido, prevê, valida o render **olhando um frame** e exporta. Não redesenha o projeto.
  - **Direitos autorais (inviolável):** `PESQUISA_FERRAMENTAS.md` §10 declara o yt-dlp "exceção aprovada caso a caso". **Analisar** (metadados/legenda) é livre; **baixar mídia** passa por um portão explícito — o botão fica desabilitado com o motivo à vista. **O portão mudou de forma em 2026-08-21, não de existência:** o cadastro de criador/autorização (`permissionCovers`, com plataforma e prazo) saiu com o pipeline e foi substituído por `ytFetchGate(YT)` — uma **declaração marcada pelo operador** (`.vop-yt-rights`, "Declaro que tenho autorização do criador…"), válida por sessão e **por URL** (trocar de vídeo exige declarar de novo). É conferida DUAS vezes: antes de chamar e na volta da resposta, porque a declaração pode ser desmarcada durante os ~50 s do download. **É mais frouxo que o cadastro antigo — não tem prazo nem plataforma; se um dia precisar de prova, o registro volta como tabela, não como checkbox.** Nunca remover o portão. Nunca `--exec`, `--netrc-cmd`, cookies de navegador nem aria2c; `--ignore-config` para nenhum `yt-dlp.conf` da máquina reintroduzir o proibido; e a URL entregue ao processo é reconstruída do id validado, nunca a string colada.
  - **Armadilha medida (não re-descobrir):** `--download-sections` obriga o downloader FFmpeg, que refaz o pedido com User-Agent de Chrome; o cliente padrão `ANDROID_VR` devolve URL casada com outro agente e o YouTube responde **403**, que aparece como `ffmpeg exited with code 3436169992`. Só `web_embedded` entrega 1080p — `tv`/`web`/`android`/`mweb`/`tv_simply` caem em 360p e `ios`/`web_safari` não negociam formato. Fixado `player_client=web_embedded,tv,web`.
  - **Precisa de `http://127.0.0.1:8765`** (rode `estudio.ps1`); em `file://` as rotas não existem. Checks: `py -3.12 video-worker/test_ytclip.py` (124) · `test_serve.py` (53) · `test_worker.py` (89) · `node test-video-ops.js` (32) · `node test-video-ops-dom.js` (70) · `node studio/test-preset.mjs` (45). **`test-video-ops-e2e.js` e `test-ytclip-ui.js` foram APAGADOS em 2026-08-21:** o primeiro provava o pipeline (variantes, `result.json`, aprovação por hash), assunto que deixou de existir — o lado do worker segue coberto por `test_worker.py`; o segundo cobria a ponte YouTube dentro da aba `Material`, e as provas dela (lista de trechos, prévia sem download, portão de direitos) vivem agora em `test-video-ops-dom.js`.
  - **Foco editorial + preset BUSINESS_SERIOUS (decisão do usuário, 2026-08-18):** o alvo são cortes de podcast de **negócios/empreendedorismo em PT-BR** (dinheiro, carreira, liderança, disciplina, erro, mentalidade). A direção é *"conteúdo forte → corte certo → comunicação clara → edição discreta → autoridade"*, **nunca** "efeito, efeito, efeito". Proibido por escrito: meme, emoji, estética de desenho, texto em movimento constante, zoom agressivo, shake, corte a cada segundo, B-roll aleatório, música alta, cor neon, "edição genérica de TikTok" e **qualquer efeito disparado só porque o tempo passou**.
    - **Detector (`ytclip.py`) classifica**: `classify_segment()` puro, léxico PT-BR (+ inglês reduzido) em 13 categorias (`business, mindset, money, career, leadership, discipline, experience, failure, success, advice, life_lesson, strong_opinion, reflection`) → campo `category`. **É palpite tirado das PALAVRAS da legenda, nunca medição** — por isso NÃO entra em `signals`, que é reservado a evidência (heatmap/capítulo/fala).
    - **Corte autocontido**: `_dangling_opener()` detecta trecho que abre com conector solto (`mas`, `então`, `aí`, `isso`, `porque`…) e o `_window` recua para um começo limpo; quando não dá, sai `contextWarning` em português e o operador vê o aviso na lista. **Duração dirigida pela IDEIA, não por cronômetro**: fecha em fim de frase + pausa (`CLOSE_PAUSE_SEC`) ou troca de assunto — 25s para uma tese curta, ~70s (`STORY_CLIP_SEC`) para uma história, teto duro em `MAX_CLIP_SEC`.
    - **Editor (`studio/src/preset.js` + `Clip.jsx`)**: todos os números em `TOKENS`. Legenda Inter Bold 58px branca, **máx. 2 linhas**, 820px de largura (passa por baixo da trilha do TikTok); a posição vertical era topo em 66% da altura e **saiu do preset em 2026-08-27**, virando âncora pela base do retângulo do vídeo, calculada em Python (ver o fim desta seção); fala longa vira páginas curtas com tempo proporcional ao texto. **Ênfase: no máximo UMA palavra por página**, por significado (termo de dinheiro/erro ou número com unidade) — cor amarelo queimado `#D9A441`, verde `#8FB573` para dinheiro/sucesso, vermelho `#C0554A` para fracasso, **uma cor por vez**. Título opcional em Montserrat ExtraBold. Fontes via `@remotion/google-fonts`.
    - **Legenda NÃO anima, de propósito** (skill `emil-design-eng`: o que o espectador vê dezenas de vezes não deve animar; e a spec proíbe "constant text movement"). Movimento só com razão semântica.
    - **Armadilha medida (não repetir):** `transform: scale(1.05)` numa palavra destacada cresce o glifo mas **não a caixa de layout** — em palavra longa os 5% transbordam ~17px e comem o espaço seguinte ("Faturamentonão"). Só aparece OLHANDO O FRAME. A escala foi removida; ênfase é cor + peso 900.
    - ~~**`TOKENS.videoEscala` = 1.45 é o botão de calibragem**: corta 270px de cada lado, bom para plano fechado, pode cortar convidado na ponta em plano aberto — baixar para ~1.15 nesses casos.~~ **SUPERADO em 2026-08-26** (ver o bullet do fundo por miniatura, abaixo): `videoEscala` virou **1.0**, fonte inteira sem corte. E o corte nunca foi de 270px — 1.45 recortava **243px de cada lado** (1080 − 1080/1,45, dividido por 2).
    - Checks: `node studio/test-preset.mjs` (45) · `py -3.12 video-worker/test_ytclip.py` (102).
  - **Fora da v1 de propósito:** palavra destacada, zoom, transição, corte dinâmico, trilha, efeito sonoro, marca, abertura/encerramento. A base tinha que renderizar certo antes de ganhar recurso. **Render de 10 s leva ~150 s** — quase tudo é o `npx remotion render` refazendo o bundle; se incomodar, o caminho é `@remotion/bundler` com bundle reaproveitado, não mexer na composição.
  - **Armadilha medida — teto de tempo FIXO mata clipe longo calado (2026-08-26):** relatado como "espero carregar e no fim não baixa o vídeo" num trecho de 90 s. Não era o Remotion nem o download: `DEFAULT_RENDER_TIMEOUT` eram **900 s fixos** e o `subprocess.run` morria no meio do render — a rota devolvia 500 e a tela mostrava só um toast de erro depois de 15 min de espera. **Medido ponta a ponta nesta máquina (i5 de 6 núcleos, 1080x1920): 90 s de clipe = 2700 quadros em 1005 s** (~0,37 s por quadro, ~11 s de render por SEGUNDO de clipe; MP4 de 39,7 MB). O custo é por quadro, então **teto constante é errado por construção**: sobra para 10 s e mata todo clipe acima de ~80 s. Agora `serve.render_budget(duração, piso)` é proporcional (`RENDER_FIXED_SEC` + duração × `RENDER_SEC_PER_CLIP_SEC`) e `DEFAULT_RENDER_TIMEOUT` virou **piso** — a assinatura de `build_server` não mudou. `RENDER_SEC_PER_CLIP_SEC` é o botão de calibragem e guarda o DOBRO do medido de propósito (ver a armadilha dos órfãos abaixo). **Não é a concorrência nem o blur:** medido, `--concurrency=6` tira 17% e remover o `blur(44px)` tira ~14% — o custo é o decode DUPLO por quadro (fundo borrado + vídeo) num Chrome headless, e nenhum dos dois muda a ordem de grandeza. Se um dia incomodar de verdade, a saída é o caminho FFmpeg que já existe (mesmo 9:16 com legenda queimada, em segundos; só a ênfase colorida por palavra é exclusiva do Remotion) — não afinar a composição. A tela também parou de prometer "leva alguns minutos": `renderEta()` estima com o número medido (BP-008), e a recusa por tempo aponta o download do Passo 3 em vez de só dizer que falhou. Checks: `test_serve.py` bloco 25 (160) · `node test-video-ops.js` (38).
  - **Armadilha medida — render interrompido deixa Chrome órfão e derruba o render SEGUINTE (2026-08-26):** matar um `npx remotion render` no meio **não** mata os `chrome-headless-shell` filhos. Medido: 7 órfãos segurando **1,9 GB** depois de duas interrupções, com a máquina caindo para 3,0 GB livres. O render seguinte renderizou os 2700 quadros e morreu na JUNÇÃO com `FFmpeg quit with code 3221225794` (`0xC0000142` = falha de init de DLL no Windows, sintoma clássico de processo novo sob exaustão de memória) e saída do FFmpeg **vazia**. Limpos os órfãos (5,1 GB livres), o MESMO render passou em 1005 s. **O erro não aponta a causa** — parece defeito de FFmpeg ou de codec e é falta de RAM. Antes de investigar `0xC0000142`, rodar `Get-Process chrome-headless-shell` e limpar; e um controle curto (`--frames=0-59`) pelo mesmo caminho separa "escala" de "ambiente" em 46 s. Nada disso é código a corrigir — é o que conferir primeiro.

  - **Ponte Baixador → Estúdio: "Mais reproduzidos" + legenda no 9:16 (decisão do usuário, 2026-08-24):** liga a extensão do `baixador/` (porta **8770**, mundo separado do Estúdio) ao Passo 2 e ao render 9:16. O caminho é: **aba do YouTube → extensão → helper → yt-dlp → sidecar → Estúdio → corte recomendado → 9:16 com legenda queimada**.
    - **Aba atual (`baixador/extension/`):** a permissão foi de `[]` para **`["activeTab"]`** e nada mais — sem content script, sem service worker, sem `<all_urls>`, sem histórico. O popup lê a aba no clique do ícone, valida com `ytWatchId()` (host fechado + id de 11 caracteres, mesma regra do `ytclip.video_id`) e **preenche o campo**; digitar/colar URL na mão continua igual. Aba que não é vídeo do YouTube diz isso por escrito (BP-008). A URL entregue ao yt-dlp é **reconstruída do id validado**, nunca a string da barra.
    - **Portão de direitos na extensão (novo):** o popup **não tinha portão nenhum** — baixava com um clique. Agora tem a mesma declaração do `ytFetchGate` do Estúdio: checkbox `#direitos`, botão travado até marcar, e a declaração vale **por URL** (`urlAutorizada`) — trocar de vídeo desmarca. Conferida DUAS vezes, como no Estúdio: ao habilitar e no clique. `falha()` e o fim do download liberam por `atualizaBotao()`, nunca por `disabled = false` (senão um erro reabriria o portão). **Armadilha medida (corrigida na revisão de 2026-08-24):** marcar a caixa com o campo VAZIO — o caso comum quando a aba não é do YouTube — deixava a declaração sem alcance e a PRÓXIMA URL colada a herdava, liberando o download sem declarar para ela. A causa era `urlAutorizada` ser atribuída dentro do `atualizaBotao()` (que roda nos dois eventos) e o guarda só disparar com `urlAutorizada` truthy. Agora **marcar a caixa é o ato de declarar**: a atribuição vive só no `change` do checkbox, e o `atualizaBotao()` virou validador puro que desmarca em QUALQUER divergência.
    - **Sidecar v2 (`<vídeo>.mostreplayed.json`):** ganhou `"version": 2` e o bloco `captions` (`available/language/kind/reason/note/cues`) ao lado do `mostReplayed` que já existia. As falas saem do **MESMO `.info.json`** que o `--write-info-json` já gravava — UM GET no json3 que o próprio dump aponta, **zero chamada extra ao yt-dlp**. Sidecar antigo (sem `version`, sem `captions`) **continua valendo** e é lido como "vídeo sem legenda"; ninguém rebaixa vídeo para migrar formato.
    - **Uma extração de legenda só:** o bloco inline do `probe()` virou **`ytclip.fetch_cues(info)`**, chamado pelo Estúdio E pelo baixador. `pick_caption_track` teve o **aninhamento invertido — IDIOMA por fora, origem por dentro**: antes uma legenda MANUAL em espanhol vencia uma AUTOMÁTICA em pt-BR e o corte saía legendado em espanhol. Dentro do mesmo idioma, humano continua ganhando da automática. `CAPTION_LANGS` não mudou.
    - **Recomendação continua recomendação:** `/api/most-replayed` passou a devolver também um **resumo** da legenda (`available/language/kind/reason/count`) — **nunca as falas**: um podcast de 3 h tem milhares de cues e a tela só precisa saber que existe legenda. O aviso aparece UMA vez no cabeçalho de `mrListHTML` (legenda é do vídeo, não do pico) e também quando não há pico nenhum, porque o corte marcado na mão também sai legendado. **Nada disso cria corte sozinho** — `mrSelectionFrom` segue devolvendo só `inSec`/`outSec`.
    - **Legenda queimada, só no 9:16:** `video-worker/captions.py` (**novo, puro, stdlib**) converte cues em **ASS**. Os números NÃO são novos — são os do `studio/src/preset.js` (`TOKENS`): Inter Bold 58px branca, **máx. 2 linhas**, topo em 66% da altura, 820px úteis, sem contorno, sombra de leitura. `test_captions.py` **lê o `preset.js`** e compara, então divergir entre os dois renderizadores falha em vez de passar calado. O recorte e o rebase são do `ytclip.cues_for_range` (fronteira única). `worker.build_filter(reframe, ass)` só **acrescenta `,ass=<nome>`** no fim da cadeia que já existia; `render_cut(..., ass_file=)` passa a pasta como `cwd` do FFmpeg **para o filtro receber só o NOME** — escapar `C:\...` dentro de filter_complex é a armadilha clássica do Windows, e o nome vem do token (`TOKEN_RE`), então não há o que escapar. **O horizontal não é tocado** e não ganha legenda.
    - **Armadilha medida (não re-descobrir):** o **libass NÃO falha quando não acha a fonte** — ele troca calado. Medido nesta máquina com `-loglevel info`: `fontselect: (Inter, 700, 0) -> Arial-BoldMT`. O corte sai legendado do mesmo jeito, em Arial, e ninguém fica sabendo. Por isso existe `captions.font_available()` e o estado **`burned-sem-inter`**, que diz na tela que a tipografia caiu para Arial. ~~**Instalar a fonte Inter é o que resolve**~~ — **SUPERADO em 2026-08-28:** a fonte agora vai **empacotada** no repo (`video-worker/fonts/Inter-Bold.ttf`) e o libass a recebe por `fontsdir` (ver o bullet do fim desta seção). Instalar no Windows deixou de ser necessário; nunca mexer no ASS.
    - **Estado sempre visível (BP-008):** `_send_video` emite **`X-Clip-Captions`** (mesmo mecanismo do `X-Clip-Path`) com `burned` | `burned-sem-inter` | `CAPTIONS_NOT_AVAILABLE` | `CAPTIONS_EXTRACTION_FAILED` | `CAPTIONS_OUT_OF_RANGE`; o `captionsMessage` do `video-ops.js` tem uma frase para cada. Sem cabeçalho (perfil horizontal) não há o que dizer. Os pares `NOT_AVAILABLE` × `EXTRACTION_FAILED` **nunca** colapsam num erro genérico, nem para audiência nem para legenda. **O estado sai de um conjunto FECHADO (`serve.CAPTION_STATES`, revisão de 2026-08-24):** o `reason` vinha do sidecar em disco e ia cru para o cabeçalho HTTP — um `reason` com CRLF injetava cabeçalho na resposta, e um `reason` desconhecido não tinha frase no `CAPTIONS_MSG` e o corte saía sem legenda com a tela muda (BP-008). `_caption_state()` recusa o que não é conhecido tratando-o como `CAPTIONS_EXTRACTION_FAILED` — **sem** colapsar os dois motivos legítimos, que continuam distintos.
    - **Fora desta versão de propósito:** ênfase semântica de palavra no ASS (o `pickEmphasis` é do Remotion; portá-lo seria um segundo sistema de NLP noutra linguagem) e legenda no horizontal.
    - Checks: `py -3.12 video-worker/test_captions.py` (76) · `test_serve.py` (130) · `test_ytclip.py` (138) · `baixador/local-helper/test_helper.py` (127) · `node baixador/extension/test-popup.js` (58) · `node test-video-ops-rec.js`.

  - **Fundo por miniatura, legenda revisável e fim da sobreposição (decisão do usuário, 2026-08-25):** três defeitos vistos num corte 9:16 REAL, corrigidos sem mexer no fluxo (YouTube → extensão → helper/yt-dlp → legenda + Mais reproduzidos → sidecar → Estúdio → render). O horizontal e o Remotion não foram tocados.
    - **O fundo desfocado virou a MINIATURA do vídeo, escurecida.** O desfoque nunca foi o desfoque do quadro: era o desfoque da **tira central** dele (`scale=108:192:increase,crop`, ou seja 32% da largura de um 16:9 esticados para 1080). Num podcast, o topo dessa tira é o teto/fundo escuro do estúdio e a base é a mesa iluminada — daí a tarja de cima sair quase preta e a de baixo, não. Agora `worker.build_filter(reframe, ass, background)` monta `[1:v]…increase,crop=1080:1920,eq=saturation,colorchannelmixer[thumb]` sobre o quadro inteiro, com o vídeo por cima em `scale=decrease` (proporção intacta). **Sem `background` o filtro é EXATAMENTE o de antes** — MP4 local e lote continuam no desfoque.
      - **`[canvas]` não é enfeite:** o `overlay` produz um quadro por quadro da entrada de BAIXO, e a miniatura tem um quadro só (25 fps por padrão do decodificador de imagem). Com a miniatura embaixo, um original de 30 fps sairia reamostrado para 25. O `[0:v]split` dá o relógio; a miniatura cobre 100% do canvas, então o conteúdo dele é irrelevante. Provado pelo check `22g` (fps da saída == fps da fonte).
      - **Escurecer é MULTIPLICATIVO (`THUMB_LUZ = 0.42`), nunca `eq=brightness`.** Medido olhando frame: `brightness=-0.50` tira 128 de 255 de todo pixel e uma miniatura escura — metade dos podcasts — virava preto puro, matando a razão de o fundo existir. `THUMB_LUZ`/`THUMB_SATURACAO` são o botão de calibragem.
      - **Miniatura ilegível NÃO derruba o corte:** `serve.background_ok()` abre a imagem antes (um processo de ~0,1 s) e, falhando, volta o desfoque com o motivo no console. Sem isso, um `.webp` truncado fazia o FFmpeg abortar e o corte inteiro virava 500 — visto no teste, não deduzido.
      - **Aquisição:** `--write-thumbnail` no MESMO comando yt-dlp que já roda (nenhum download escrito à mão, nenhuma permissão nova na extensão, nada de `--convert-thumbnails`, que exigiria FFmpeg no PATH do helper). O sidecar foi para `"version": 3` com a chave `thumbnail` = só o NOME do arquivo; quem resolve a pasta é o `serve.cut_background`, com a mesma guarda de travessia do sidecar mais lista fechada de extensão. **Sidecar v1/v2 continua valendo** — sem a chave, ~~fundo desfocado~~ **letterbox na cor do preset** (o desfoque saiu em 2026-08-27).
    - **Legenda revisável antes do render (`POST /api/clip-captions`).** A automática do YouTube erra palavra e censura palavrão como `[ __ ]`: no Passo 3, o botão **Legenda** abre a lista de falas do trecho, com o tempo em LEITURA e o texto editável. Nenhuma requisição nova ao yt-dlp e nenhum byte de rede — as falas já estão no sidecar. A rota faz as duas pontas: sem `cues` no corpo, devolve o que o YouTube detectou; com `cues`, guarda a correção. A correção vive na sessão do servidor, chaveada por **token + intervalo** (`serve.edit_key`) — token sozinho vazaria a legenda de um corte para outro do mesmo podcast, e mudar o intervalo invalida a correção de propósito. **O sidecar em disco nunca é reescrito:** ele continua sendo o registro do que o YouTube disse, e a correção é uma camada por cima. O `⬇ Baixar vídeo` sobe a correção ANTES do render (o corpo do `/api/video-cut` é o vídeo, não sobra lugar para texto lá) e falha o download se isso falhar — baixar com a legenda errada depois de corrigida seria pior. Nada é adivinhado: palavra faltando continua faltando até uma pessoa escrever.
    - **Sobreposição de cue morreu na fronteira de dados, não no CSS.** A legenda automática é ROLANTE: a fala de agora continua no ar quando a próxima começa (medido por volta de 13,25 s de um corte real), e os dois `Dialogue` caíam na mesma região da tela. `captions.normalize_cues()` é a regra única — ordena por (início, fim, texto), joga fora o inválido, **fecha a fala anterior no começo da seguinte**, resolve empate de início pela última e descarta o que sobrar curto demais para ler. É chamada pelo `ytclip.cues_for_range` (fronteira única de cue de clipe: pega o ASS **e** as Sequence do Remotion) e de novo pelo `captions.to_pages` (que é por onde passa a legenda CORRIGIDA, que não vem do `cues_for_range`). **`parse_json3` NÃO foi tocado de propósito:** o detector de cortes usa o intervalo entre falas para achar pausa, e fechar cue lá mudaria a recomendação — que estava fora do pedido.
    - **A legenda saiu da tarja e entrou no vídeo.** O topo fixo em 66% da altura (1267 px) caía **quatro pixels abaixo** de um 16:9 deitado no 9:16 (o vídeo vai de 656 a 1264): a legenda era escrita no fundo, não sobre a imagem. Agora `captions.to_ass(..., video_h=)` ancora pela BASE (`Alignment 2`) a `RODAPE_PCT` da borda de baixo do vídeo, com `ZONA_UI_PCT` impedindo que o perfil `crop` jogue o texto na faixa de botões do TikTok. `serve.video_box(profile, media)` calcula o retângulo com as dimensões que o `probe` já leu. **Ancorar pela base é o que faz uma página de duas linhas crescer para cima em vez de vazar para fora do vídeo.** A tipografia continua espelhando o `preset.js` (bloco 6 do teste); só a POSIÇÃO divergiu, porque o palco do Remotion ampliava o vídeo em 1,45× e o descentralizava. **Essa divergência morreu em 2026-08-27** (ver o fim desta seção): a ampliação saiu em 2026-08-26 e a âncora virou uma conta só, em Python, que o Remotion recebe pelo prop `legendaBase`.
    - **Armadilhas medidas na revisão adversarial desta mesma entrega (não re-descobrir):**
      - **O `.ass` não pode ser nomeado só pelo token.** O token é da SESSÃO (um por vídeo aberto), não do corte: com `<token>.ass` fixo, dois downloads simultâneos do mesmo vídeo escreviam no MESMO arquivo — medido com servidor e FFmpeg reais: o clipe A saiu **200, "burned", com a legenda do clipe B**, e o B morreu com 500 porque o `finally` do A apagou o arquivo debaixo dele. Agora o nome leva um sufixo aleatório. **Não trocar isso por "gravar dentro do render_lock"**: o lock é declaradamente removível ("se um dia houver máquina sobrando, tire o lock") e a corrida voltaria calada. Regressão em `test_serve.py` bloco 24 (dois downloads em threads) — provado que ele FALHA com o nome antigo.
      - **A correção é consumida no uso (`caption_edits.pop`).** Guardada, ela sobrevivia ao "Restaurar texto do YouTube" e ao corte apagado e remarcado no MESMO intervalo: o download seguinte queimava um texto que ninguém mais queria, calado. O navegador reenvia a correção antes de cada download, então consumir não perde nada.
      - **`if override is not None`, nunca `if override:`.** Apagar o texto de todas as falas é o gesto de "não quero legenda"; com o teste de verdade a lista vazia caía no ramo do sidecar e o corte saía queimando de volta **exatamente o texto apagado**, com a tela dizendo "com o texto que você corrigiu". Daí o estado `CAPTIONS_EDITED_EMPTY`.
      - **`CAPTIONS_TOO_MANY` fica FORA de `CAPTION_STATES`:** ele é resposta da rota de revisão, não desfecho de render — um estado que pode sair no cabeçalho sem frase do outro lado é o erro mudo que o conjunto fechado existe para impedir. E `test_serve.py` (21t0/21t1) **lê o `video-ops.js`** e falha quando um estado não tem frase lá: foi essa prova que pegou o `CAPTIONS_EDITED_EMPTY` sem mensagem.
      - **A revisão recusa trecho com mais de `MAX_EDIT_CUES` falas** em vez de mostrar as primeiras como se fossem todas — corrigir uma palavra viraria override truncado e o resto do clipe sairia MUDO, pior que não revisar.
      - **"Este trecho está renderizando" mora no módulo (`CUT_BUSY`), não no botão.** O botão novo ("Legenda") re-renderiza a lista, então o `disabled` posto no elemento voltava habilitado no meio do download e um segundo clique subia o original de novo e disparava um segundo FFmpeg. É a mesma razão já escrita no `ytBusy`.
      - **`capRestore` escreve nos `<input>` que já estão na tela**, sem re-render: a lista de falas é um container rolável próprio e um `innerHTML` novo a jogaria para o topo (BP-013 — o `renderKeepingScroll` só cuida do scroll da janela).
      - **Descartar a revisão fecha o painel** (`capDrop`), senão a linha ficava com "Fechar legenda" e `aria-expanded="true"` sobre um painel que não é mais renderizado; e o descarte por mudança de intervalo é **dito no toast do Passo 2**, onde o operador agiu.
    - **Fora deste pedido de propósito:** o caminho Remotion (`/api/yt-fetch` → `/api/remotion-render`) ganhou só o fim da sobreposição, que vem de graça do `cues_for_range`; fundo por miniatura e âncora geométrica lá exigiriam passar a proporção da fonte nas props e recalcular o palco, e a tela revisada é a do sidecar.
    - Checks: `py -3.12 video-worker/test_captions.py` (76, inclui a regressão do 13,25 s) · `test_serve.py` (152) · `test_ytclip.py` (138) · `test_worker.py` (89) · `test_helper.py` (127) · `node test-video-ops.js` (37) · `test-video-ops-dom.js` (70) · `studio/test-preset.mjs` (45).
  - **Fundo por miniatura TAMBÉM no Remotion, e o vídeo parou de ser cortado (decisão do usuário, 2026-08-26):** o 9:16 do Remotion ainda usava o fundo do próprio vídeo **ampliado e borrado**, com o vídeo em **1,45×** — o que cortava **243px de cada lado da fonte** e podia decapitar convidado na ponta de um plano aberto. Agora a camada é `miniatura escurecida → vídeo → legenda`: `Img` do Remotion (não `<img>` — o `Img` segura a captura com `delayRender` até a imagem carregar, senão os primeiros quadros saem sem fundo e isso só aparece OLHANDO o frame 0), `objectFit: cover` (**só a miniatura é recortada; o vídeo nunca**) e `brightness(TOKENS.fundoLuz) saturate(TOKENS.fundoSaturacao)` = **os mesmos 0.42/0.55 do `worker.THUMB_LUZ`**, com prova de paridade no `test_serve` bloco 26 lendo o `preset.js` — se um dos dois mudar sozinho, o mesmo corte sairia com fundo diferente em cada caminho, calado. `videoEscala: 1.45 → 1.0` e `videoCentroPct: 0.40 → 0.47`.
    - **Aquisição: `--write-thumbnail` no MESMO comando yt-dlp que o `fetch_section` já roda.** Sai da lista `thumbnails` do dump que o extractor já fez — nenhuma chamada extra, nenhum cliente HTTP novo, nenhum segundo pipeline. Descartado baixar a URL de miniatura do `probe` por `urllib`: seriam ~15 linhas novas em `serve.py` (extensão pelo content-type, teto de bytes, tolerância a falha) para produzir o MESMO arquivo na MESMA pasta, e criaria um segundo caminho de rede num módulo cuja regra é que só o yt-dlp fala com o YouTube.
    - **Descoberta por stem numa função só:** `ytclip.thumbnail_beside(pasta, arquivo)` devolve **NOME** (é o que o `staticFile()` espera, e impede caminho de disco de vazar nos props), pula candidato de 0 byte em vez de desistir no primeiro ruim, e o `serve.THUMB_EXTS` passou a **ser** o `ytclip.THUMB_EXTS` (duas listas divergiriam e o corte perderia o fundo dependendo de por onde entrou). O `background_ok`, que já existia para o FFmpeg, filtra imagem ilegível ANTES do render.
    - **Armadilha medida — a miniatura ao lado quebrava o DOWNLOAD:** o arquivo produzido era escolhido por `sorted(glob(stem + ".*"))[0]`, ordem alfabética. Com a imagem na mesma pasta, **`.jpg` vem ANTES de `.mp4`** e o `validate_input` receberia a imagem: o download inteiro morreria com "sem faixa de vídeo". Fragmento (`.f616.mp4`) e sobra (`.part`) também ordenam na frente. Virou `ytclip.produced_media()`, que prefere o nome exato `<stem>.mp4` — extraída para função justamente para o teste exercitar o caso com arquivos reais em vez de asserir texto (checks 16a–16c).
    - **Armadilha medida — asserir flag proibida no TEXTO do arquivo reprova a documentação:** o check "nenhuma flag proibida entrou" falhou porque `--exec`, `cookies` e `aria2c` aparecem nos comentários que dizem que elas são proibidas. O teste passou a interceptar o `ytclip._run` e asserir o **argv real** (checks 16h–16k), o que também prova que é **um comando só**.
    - ~~**O ramo sem miniatura fica EXATAMENTE como estava (desfoque).** Não é reintroduzir blur: é a mesma condição em que o `worker.py` mantém o perfil `blur` (fixado pelo check 20h).~~ **SUPERADO em 2026-08-27** (ver o bullet da legenda ancorada pela base, no fim desta seção): esse ramo virou letterbox chapado e o desfoque deixou de existir no projeto. O 20h continua lá, mas agora afirma outra coisa (`background=None` e sem argumento dão o mesmo filtro). O resto deste parágrafo continua valendo: A alternativa medida — o próprio vídeo em `cover` — ampliaria a fonte **3,16×** (recorta 68% da largura de um 16:9 num 1080x1920): zoom NÍTIDO maior que o 1,45× que este pedido acabou de remover, e "zoom agressivo" é proibido por escrito no BUSINESS_SERIOUS. Alcançado por token de upload local, trecho em cache de antes da mudança e falha do `--write-thumbnail`.
    - **`videoCentroPct` controlava a folga da legenda — até 2026-08-27, quando a âncora por percentual saiu:** enquanto a legenda era pendurada pelo TOPO num percentual fixo do quadro (`legendaTopoPct`), descentralizar o vídeo era o único jeito de abrir folga embaixo dela. Medido na época: com escala 1 e centro em 0.40 o vídeo terminava em 1072 e o texto começava em 1267 (**195px de deriva**, contra 58,6px antes); 0.47 punha a base em 1206 e a folga em 61px; e **0.50 (centro geométrico exato) encostava a legenda a 3,5px da borda do vídeo**, por isso o check 1d exigia `videoCentroPct < 0.5`. **Hoje nada disso vale:** a legenda é ancorada pela BASE do retângulo do vídeo (conta única em `captions.margem_inferior`, que chega ao Remotion pelo prop `legendaBase`), a folga sai da própria conta (8% da altura do vídeo) e `videoCentroPct` virou **0.50** — o mesmo enquadramento do FFmpeg (`overlay=(W-w)/2:(H-h)/2`) —, com o 1d reescrito para exigir `=== 0.5`. Provado por aritmética no `test-preset.mjs` (6a–6d), não por render.
    - **Fora deste pedido de propósito:** posição/estilo de legenda, o caminho FFmpeg (que já fazia isso), e um cabeçalho `X-Clip-Background` dizendo na tela qual fundo saiu — o motivo do fallback vai só ao console do servidor, então **é o único ponto deste recurso sem estado visível na interface (BP-008)**; vale acrescentar quando alguém for mexer na tela do Passo 3.
    - **Armadilha medida na revisão adversarial desta entrega — asserção de TEXTO não prova fiação nem polaridade (não re-descobrir):** o bloco 26 nasceu todo de `in py_render`/`re.search`, e três sabotagens passaram com a suíte VERDE: (1) trocar `"backgroundFile": fundo` por `""` — **feature inteira morta**, todo corte de volta ao desfoque; (2) tirar o `not` da guarda — descarta a miniatura boa e entrega a **ilegível** ao render, que morre carregando; (3) um `_run` a mais no `fetch_section`, ou seja o segundo pipeline proibido, escapando porque o check contava `len(dict)` alimentado por **`setdefault`** (que nunca cria chave nova, então valia 1 com 1 ou com 50 chamadas). A correção não foi escrever regex melhor: foi **extrair função e exercitá-la com arquivos reais** — `serve.render_background()` (nome ou `""`) e `serve.render_props()` (o contrato inteiro dos props). Provado que cada sabotagem agora reprova: 26l pega a (1), 26i/26j pegam a (2), 16i pega a (3) e 6a pega o `videoEscala` voltando a 1.45. **Regra:** ao provar que um valor CHEGA a algum lugar, o teste tem de construir o valor; `in arquivo` só prova que alguém escreveu a palavra.
    - **Armadilha de processo — agente revisor sabota a árvore de trabalho:** a lente de regressão inverteu a guarda do `serve.py` de propósito (para medir o teste) enquanto a lente de correção lia o mesmo arquivo, e esta reportou a sabotagem como defeito **"confirmado"**, com prova empírica de execução. Ela até registrou a janela: guarda invertida às 12:08:47, revertida às 12:09:52. Descartado por três evidências independentes: o arquivo em disco, o quadro renderizado (a miniatura APARECE — com a guarda invertida sairia desfoque) e o próprio relato de horário. **Achado adversarial que afirma ter medido ainda precisa ser reconferido contra o artefato**, sobretudo quando outro agente escreve no mesmo lugar.
    - **Sabido e NÃO corrigido aqui (fora do pedido, mas real):** (a) o `render_lock` é único e compartilhado por `/api/remotion-render`, `/api/video-cut` e `/api/yt-fetch`, com `with` bloqueante e sem timeout — com o teto proporcional, um clipe de 600 s (o `MAX_CUT_SEC`) segura o lock por ~4 h e um `⬇ Baixar vídeo` do Passo 3 fica pendurado sem nada na tela dizer por quê; a saída é `acquire(timeout=…)` respondendo 409, que o `video-ops.js` já sabe tratar. (b) No ramo SEM miniatura a geometria também mudou, então corte de upload local mostra MAIS área desfocada do que antes. (c) Fonte mais alta que ~1805px é recortada no topo pelo `overflow:hidden` do `Palco` (só afeta fonte já vertical). (d) Há uma **terceira** cópia de `THUMB_EXTS` em `baixador/local-helper/yt_dlp_runner.py:236`, anterior a esta entrega.
    - Checks: `node studio/test-preset.mjs` (**56**) · `py -3.12 video-worker/test_ytclip.py` (**149**) · `test_serve.py` (**175**) · `test_captions.py` (76) · `test_worker.py` (89) · `test_helper.py` (127) · `node test-video-ops.js` (38) · `test-video-ops-dom.js` (70).
  - **Fluxo de 3 passos sobre UM vídeo por sessão (decisão do usuário, 2026-08-19):**
    - **Fonte única do original: `INTAKE`.** É o `videoSession` do pedido: `url` (blob), `file` (o File real que vai ao FFmpeg), `duration` e `cuts[]`. Os três passos montam a MESMA seção `[data-intake]` com o MESMO `<video src=INTAKE.url>` — **um player por tela, uma URL por sessão**. `bindIntake()` religa os ouvintes a cada render.
    - **Carregar só no Passo 1.** `[data-intake-input]` e `[data-intake-drop]` existem apenas em `videoStepHTML()`. Passos 2 e 3 sem vídeo mostram `needVideoHTML()` ("Carregue o vídeo no Passo 1") — **nunca** um seletor de arquivo. `intakeSetFile()` força `TAB='overview'`, porque é lá que a faixa de estado explica uma recusa (BP-008).
    - **Corte = tempo, não cópia:** `{ id, name, priority, inSec, outSec }` em `INTAKE.cuts` (os `startTime/endTime` do pedido são os `inSec/outSec` que já existiam — não foi criado modelo paralelo). Prioridade em `PRIORITY_LABEL` (`alta|media|baixa`, default `media`); `priorityOf()` normaliza valor ausente. Nome e prioridade são editados na PRÓPRIA linha (`[data-cut-field]` → `cutFieldWrite()`), **sem re-render** — reescrever a lista a cada tecla mataria o foco. Nada de descrição, legenda, hashtag ou categoria no Passo 2.
    - **Tempo digitado em min:seg (2026-08-19):** os campos Início/Fim do Passo 2 são `type="text"` e aceitam `15:30`, `1:15:30` ou segundos puros (`930`). `parseClock()` e `clockField()` são a ÚNICA fronteira — `INTAKE.inSec/outSec` seguem em SEGUNDOS, então `markIssues`, a barra, os presets e o FFmpeg não mudaram. Texto que não é tempo vira `''` (= não marcado) em vez de 0 calado. Ao sair do campo o valor volta normalizado (930 → 15:30). **O diálogo `＋ Corte` (pipeline persistente) continua em segundos.** `parseClock` é exportado e tem teste próprio em `test-video-ops.js`.
    - **Passo 3 reaproveita o mesmo player:** "Ver o trecho" é o `intake-cut-play` que já existia — posiciona em `inSec` e o `timeupdate` pausa em `outSec` (tolerância de ~0,25 s do evento; o MP4 exportado é exato). Extração física só no ⬇ Salvar, via FFmpeg local.
    - **Limite conhecido:** `INTAKE` vive na sessão — recarregar a página perde o vídeo e a lista de cortes (blob de vídeo nunca vai para o `localStorage`, por decisão de projeto). **O MP4 já baixado NÃO se perde:** ele está no disco e listado na Central. A "ponte para o pipeline antigo" (`use-session-video` → `adoptSessionVideo`) saiu junto com o pipeline em 2026-08-21.
    - Checks: `node test-video-ops-dom.js` cobre as 5 telas, a ausência de seletor de arquivo nos passos 2 e 3, o agrupamento da Central por vídeo e o portão de direitos do YouTube.
    - **Passo 3 virou `Clips` e o fluxo acaba nele (decisão do usuário, 2026-08-21):** o passo se chamava `Revisar` e tinha postura de conferência (título e prioridade em modo leitura). Agora é a lista dos clips prontos e **o fim do fluxo** — depois de baixar não existe outro passo. Cada linha tem exatamente **prévia, título, urgência e ⬇ Baixar vídeo**; `reviewCutsHTML` passou a montar a MESMA linha-editor do Passo 2 (`data-cut-field="name"`/`"priority"` → `cutFieldWrite`, sem re-render, BP-001), então nada de novo foi inventado. **Nada de revisão, aprovação, descrição, legenda, hashtag, post ou metadado neste passo** — a decisão do corte já foi tomada no Passo 2. O título digitado É o nome do arquivo: `nomeBase` do `intake-cut-save` virou `cutName(...)` (antes era `corte-N-<vídeo original>`); `cutFileName`/`safeName` continuam sanitizando e anexando o intervalo, então dois clips homônimos não se sobrescrevem. **Nomes internos NÃO mudaram** (`data-tab="review"`, `reviewStepHTML`, `reviewCutsHTML`, `INTAKE.cuts`). CSS morto removido junto: `.vop-cut-title` e `.vop-chip.vop-prio-*` (só o `reviewCutsHTML` antigo os emitia). **Horas depois, no mesmo dia, o pipeline inteiro saiu** (ver o topo desta seção) e com ele o botão "Preparar para publicação (opcional)" do Passo 2 — o que sobrou do fluxo é: baixar, e o arquivo aparece na Central.

  - **Corte baixado fica no computador (decisão do usuário, 2026-08-20; virou a base da Central em 2026-08-21):** o MP4 pronto era gravado no `mkdtemp` do servidor, servido e apagado — então o corte "sumia quando o site fechava". Agora o helper local guarda **todo** MP4 que entrega em `~/Videos/Cortes Estudio` e diz o endereço no cabeçalho `X-Clip-Path`; o navegador lê esse endereço (`savedPathOf`) e o grava em `savedPath` no registro da Central (ausente vira `''`).
    - **Um ponto só:** a gravação mora em `_send_video` (`_keep`), choke point de `/api/video-cut` **e** de `/api/remotion-render` — as duas rotas herdaram sem cópia de código. `os.replace` move (sem segunda cópia); disco diferente cai em `copyfile`. Nome repetido **sobrescreve** de propósito. Falha ao guardar **não** derruba o download: o vídeo chega igual, sem endereço, e o motivo vai ao console (BP-008).
    - **`GET /clips/<arquivo>`** é `translate_path` apontando a pasta — reaproveita GET/HEAD/404/Content-Type do `SimpleHTTPRequestHandler`. `basename` mata travessia (`/clips/../../index.html` → 404). A pasta é atributo de classe (`clips_folder`, param `clips=` do `build_server`) para o teste não sujar a pasta pessoal.
    - **Na tela:** o cartão da Central toca o corte guardado (player `controls` normal — o arquivo já É o corte, a régua não mente) e mostra o endereço. O download do navegador **continua** acontecendo: são duas cópias de propósito, a de Downloads é a de sempre.
    - **Limite conhecido:** arquivo movido/apagado por fora = player mudo, e a linha embaixo do cartão diz isso. **Quando houver banco, `savedPath` é a coluna que migra** — o vídeo continua sendo do disco.
    - Checks: `py -3.12 video-worker/test_serve.py` (53) · `node test-video-ops-dom.js` (cartão com `savedPath` toca `/clips/…`; sem `savedPath` não oferece link).
  - **O `<details class="vop-more">` sobreviveu, o conteúdo dele não:** o padrão (recolher o avançado sem JS e sem estado) continua valendo e é usado no "Como isto funciona" do Passo 2, com o modificador `.vop-more-flat` para painel que já tem moldura. Todos os OUTROS blocos recolhidos que existiam (panorama de contas, fila consolidada, metadado do corte, Drive, "depois de publicar", SHA-256, histórico, backup) saíram com o pipeline em 2026-08-21. Também saíram os rótulos daquele mundo ("post", "vídeo longo", "Revisão do post") — hoje a tela só fala de vídeo, corte e clip.
  - **Legenda ancorada pela base do vídeo nos DOIS renderizadores — uma conta só (decisão do usuário, 2026-08-27):** o FFmpeg/ASS já ancorava pela base desde 2026-08-24, mas o Remotion continuava pendurando a fala num percentual fixo do QUADRO (`legendaTopoPct` = 66% = y 1267). Depois que a ampliação de 1,45× saiu (2026-08-26), esse percentual passou a cair **~61px ABAIXO** da imagem: o mesmo corte legendado pelos dois caminhos punha o texto em lugares diferentes, e o do Remotion escrevia na miniatura escurecida em vez de sobre o vídeo. Agora **`captions.margem_inferior` é pública e dona ÚNICA da âncora vertical**: o `serve.render_props` chama ela, serializa em `props-<token>.json` e o Remotion recebe o número pelo prop `legendaBase`, que o `Clip.jsx` usa como `bottom`. **`legendaTopoPct` saiu do `TOKENS`** — não sobrou segunda fórmula para divergir. Medido: fonte 16:9 → vídeo de 607,5px (656 a 1264), `margem_inferior(1920, 608)` = **705**, base do texto em y **1215** (DENTRO da imagem), respiro de **48,75px** (os 8% do `RODAPE_PCT`, com o arredondamento inteiro do MarginV); 4:3 → **620**; 9:16 com o vídeo ocupando o quadro → **269**, onde é o `ZONA_UI_PCT` que segura o texto acima da faixa de botões do TikTok.
    - **Armadilha medida — três sabotagens do lado JS passavam com CINCO suítes verdes (não re-descobrir):** é a MESMA lição de 2026-08-26 ("`in arquivo` só prova que alguém escreveu a palavra"), reincidindo na entrega seguinte. A prova da âncora era regex no texto do `Clip.jsx`, então passavam calados: (a) tirar o argumento da tag `<Legenda>`, deixando `bottom: undefined`; (b) inverter para `altura - legendaBase`, **510px fora de lugar**; (c) tirar `legendaBase` do destructuring, **436px de erro no 9:16 em TODO corte**. A correção foi a de sempre: a guarda virou **função pura exportada (`ancoraLegenda`)** e o bloco 7 do `test-preset.mjs` a **CHAMA com valor construído** — 7e reprova (a) e (b), 7f reprova (c), e 6l reprova voltar a ancorar por `top`.
    - **Paridade do `RODAPE_PCT` com guarda DUPLA:** o valor estava copiado como literal `0.08` no `test-preset.mjs`, então trocá-lo no Python deixava as cinco suítes verdes. Agora o `test-preset` **LÊ** o `RODAPE_PCT` do `captions.py` (regex que não casa vira `NaN` e reprova o 6c) **e** o `test_captions` 6h compara o literal JS `LEGENDA_BASE_PADRAO` (**705**) com `captions.margem_inferior(1920, 608)`. As duas pontas têm de concordar, nas duas direções.
    - **Armadilha medida — `.pyc` obsoleto e VÁLIDO faz a suíte testar um módulo que não existe mais (não re-descobrir):** ao conferir esta entrega, `test_captions.py` reprovava 2 checks (`4m` e `6h`) **na máquina, não no código**. O `video-worker/__pycache__/captions.cpython-312.pyc` guardava `RODAPE_PCT = 0.2` — de uma medição que um revisor fez e desfez — enquanto o arquivo em disco tinha `0.08`, e o Python aceitava o cache como válido. **Duas coincidências alinhadas:** a validação por timestamp grava o mtime da fonte em segundos INTEIROS, e as três escritas (sabotar → compilar → reverter) caíram todas em **12:25:03**; e `0.20`/`0.08` têm o mesmo tamanho em bytes, então o `size` do header bateu também (**14591**). O sintoma acusa o código e mente com precisão: o 6h exigia **778**, que é a aritmética de 0,20. **Conserto: apagar o `.pyc`** (ou rodar com `PYTHONPYCACHEPREFIX`); não é defeito do código, não entra no commit (o `.gitignore` já ignora `__pycache__/`) e afeta só quem roda a suíte na máquina onde a coincidência ocorreu. **Antes de concluir que uma suíte Python reprovou por causa do código, conferir o cache** — mesmo reflexo do `Get-Process chrome-headless-shell` na armadilha dos órfãos.
    - **Acoplamento implícito conhecido:** `margem_inferior` assume o vídeo **CENTRADO** no quadro e nada do lado Python lê `videoCentroPct`/`videoEscala` — a garantia mora do lado JS, presa pelo 1d (`videoCentroPct === 0.5`), pelo 6a (`videoEscala <= 1`) e pela aritmética do 6b/6c. Mexer em qualquer um dos dois reprova, mas quem for unificar isso um dia deve saber que a conta não conferiu o enquadramento, ela CONFIOU nele.
    - **O desfoque acabou nos TRÊS renderizadores (mesma decisão, 2026-08-27):** com a legenda dentro do vídeo, o fundo virou moldura — e moldura desfocada é enfeite. Onde não há miniatura (MP4 local do Passo 3, trecho em cache antigo, `--write-thumbnail` que falhou), o 9:16 passou a ser **letterbox chapado na cor do preset**. `worker.BLUR_DIV`/`BLUR_SIGMA` saíram e a cadeia de 10 linhas (`split`/`gblur`/`scale`/`overlay`) virou **uma**: `scale=decrease,pad=...:color=FUNDO_COR` — a fonte continua cabendo inteira, nada é cortado. **Três cópias, uma verdade:** `worker.FUNDO_COR`, `TOKENS.fundo` do `preset.js` e o `FFMPEG_FILTERS.blur` do `video-ops.js` (o comando manual de emergência, que declara no comentário que TEM de casar com o `build_filter` — e que até agora estava falso, porque o ramo da miniatura precisa de uma SEGUNDA entrada e não cabe num `-vf` de uma linha). Paridade presa pelos checks 26p/26q, que LEEM o `preset.js`, e pelos 20j–20n, que exercitam `build_filter` em vez de procurar texto no fonte.
      - **De graça: saiu o decode DUPLO por quadro no Remotion.** Sem miniatura, o `Fundo` era um segundo `<Video>` do MESMO clipe — o Chrome decodificava duas vezes por quadro, e esse decode duplo é a maior parte dos ~11 s de render por segundo de clipe medidos nesta máquina. O check **6m CONTA as tags `<Video>`** (tem de haver exatamente uma, a do `Palco`): um `indexOf` passaria igual com duas.
      - **O nome `blur` FICA, e é para parecer errado:** ele é o rótulo em `REFRAMES`, em `serve.PROFILES`, no `FFMPEG_FILTERS`, no nome do arquivo baixado (`-9x16-blur.mp4`) e nas suítes. Renomear tocaria cinco lugares por benefício zero, então o comentário do `_reframe_chain` é o aviso para quem ler depois.
      - **Verificação visual FEITA (2026-08-27, as duas fases nos dois renderizadores).** Fonte sintética 16:9 (`testsrc2` — bordas nítidas, então dá para VER onde acaba o vídeo e onde começa a moldura), quatro quadros: página de 2 linhas e página de 1 linha, uma vez pelo FFmpeg (`build_filter("blur", ass)`) e uma vez pelo Remotion (`npx remotion still ... --frame=`). Confirmado OLHANDO: a legenda cai DENTRO do vídeo nos dois; a **base fica parada** entre a página de 2 linhas e a de 1, ou seja o bloco cresce para cima — que era todo o argumento da âncora pela base; as faixas de cima e de baixo saem em cor chapada, sem resto de imagem desfocada; e a altura do texto bate entre os dois caminhos com diferença de poucos pixels, a de métrica de fonte e não de geometria.
        - **Armadilha do PRÓPRIO teste (não re-descobrir):** `-ss` ANTES do `-i` zera o relógio da saída, então o filtro `ass` é consultado em t=0 e o quadro sai **sem legenda nenhuma**. Parece que a legenda quebrou; é o arranjo do teste. Use `-copyts` (ou ponha a fala em t=0). Custou dois quadros em branco antes de cair a ficha.
        - Nesta máquina a **Inter não está instalada**, então o quadro do FFmpeg saiu em Arial (o estado `burned-sem-inter`, que a tela já sabe dizer) enquanto o do Remotion saiu em Inter de verdade, via `@remotion/google-fonts`. As duas tipografias lado a lado é o que faz essa troca calada do libass ficar óbvia.
  - **A fonte Inter virou parte do repo, e as provas viraram UM comando (2026-08-28):** os três pontos que sobraram da entrega anterior, consertados pela raiz.
    - **Fonte EMPACOTADA em vez de instalada.** `video-worker/fonts/Inter-Bold.ttf` (release oficial, SIL OFL, `LICENSE.txt` ao lado) + `fontsdir` no filtro `ass`. O corte sai em Inter em qualquer máquina e em qualquer container, sem passo manual — e a nuvem deixou de precisar de fonte instalada. **Prova medida, com polaridade:** sem a fonte ao lado, `fontselect: (Inter, 700, 0) -> Arial-BoldMT`; com ela, `-> Inter-Bold`.
      - **O ARQUIVO importa, e o nome dele não prova nada:** lendo a tabela `name`, só o `Inter-Bold.ttf` do `extras/ttf/` se declara família **"Inter"**; o `Inter-ExtraBold.ttf` se declara família **"Inter ExtraBold"** e o libass NÃO o casaria com o `(Inter, 700)` do estilo — cairia em Arial calado, o mesmo defeito. Por isso o check `7d` do `test_captions` **lê a tabela `name`** com `struct` do stdlib.
      - **`fontsdir=.`, nunca o caminho da pasta:** `worker._place_font` copia a fonte para AO LADO do `.ass`, porque o valor vai DENTRO do `filter_complex` e caminho absoluto do Windows exige escapar `\` e `:` — a armadilha que o `ass=` já evitava rodando com `cwd` na pasta. 420 KB por corte, contra megabytes de vídeo. Arquivo ausente não levanta (é o `burned-sem-inter` da tela); falha de CÓPIA levanta, porque engolir devolveria Arial calado.
      - `font_installed()` virou **`font_available()`**: a pergunta mudou de "está instalada neste Windows?" para "o arquivo empacotado está lá?" — a primeira nunca foi respondível na nuvem. Saíram com ela os checks de varredura por prefixo (o caso "Interstate" não passar por "Inter"): não há mais nome de terceiro para casar errado.
    - **`provas.ps1`: um comando para as oito suítes, e a documentação deixou de poder mentir.** Resolve dois defeitos com a mesma raiz — não existia comando que rodasse as provas, então cada um rodava à mão e **somava de cabeça**. (1) `PYTHONPYCACHEPREFIX` num temporário: o `__pycache__` da árvore não é consultado e a armadilha do `.pyc` obsoleto **deixa de ser possível**, em vez de virar "lembre-se de conferir". (2) Quem soma é o script, que **compara o total com a linha `Checks:` do `CLAUDE.md` e sai com erro se divergir**, imprimindo a linha pronta para colar. Provado na própria estreia: ele reprovou com "o CLAUDE.md diz 809 e o medido agora é 812" e, antes disso, pegou três quebras que um grep meu havia deixado passar. A conferência falha alto se não achar a linha — conferência que não acha nada é pior que nenhuma.
  - **A legenda parou de apagar fala (decisão do usuário, 2026-08-28):** relatado como "a legenda não está sabendo ler o que está sendo reproduzido" e "não completa os assuntos". NÃO era a fonte: eram **duas regras do `normalize_cues` que descartavam texto**, as duas caladas. MEDIDO com três falas de podcast: **22 palavras viravam 14 (-36%)** e a frase "eu perdi quarenta mil reais no primeiro ano" desaparecia inteira.
    - **Regra do empate (era `while saida and cue["start"] <= saida[-1]["start"]: saida.pop()`).** Como a lista já vem ordenada, o `<=` só dispara em **empate exato** — raio estreito, dano total. A regra estava certa para a ASR REESCREVENDO a mesma fala (`"eu acho"` -> `"eu acho que sim"`) e errada para duas falas DIFERENTES no mesmo instante. `_reescrita()` separa os dois casos **por prefixo**: reescrita colapsa na versão mais completa; falas diferentes **repartem o intervalo** por tamanho de texto. Intervalo apertado demais para duas falas legíveis **junta os textos** — página mais longa é pior que o ideal, frase apagada é pior que isso. `_resolve_empate` trabalha em GRUPO, não par a par, porque a decisão depende de todas as falas do instante.
    - **Regra 5 (a fala encurtada).** Quando a sobreposição deixava a anterior abaixo do piso de leitura, ela era descartada ("buraco curto é melhor que piscada") — e com legenda rolante isso acontece MUITO, cada descarte levando as palavras junto. Agora o texto dela **entra na fala seguinte**.
    - **A correção manual passava por aqui também.** O `to_pages` chama o `normalize_cues`, então o pipeline apagava linha **depois** de a pessoa ter corrigido no painel do Passo 3. Por isso este conserto é pré-requisito de qualquer tela de revisão nova — e também de trocar a fonte por transcrição local.
    - **É Python-only, e de propósito:** não existe `normalize_cues` em JS. Os dois renderizadores 9:16 recebem cue que já passou pelo `ytclip.cues_for_range`, que chama esta função — um conserto, os dois caminhos.
    - **Legenda editável no trecho recomendado, também no caminho do Remotion (2026-08-28):** era o último ponto antes de um render de minutos em que uma pessoa podia consertar palavra errada ou `[ __ ]`, e não existia. **Nenhuma rota nova:** o `/api/remotion-render` já recebe as falas NO CORPO da requisição, e as falas do trecho já chegaram em `clip.clipCues` pela resposta do `/api/yt-fetch` — então editar a sessão basta, e o `capEdited(clip)` vence o original na hora de renderizar. O painel é **o mesmo do Passo 3**: `capHTML`/`capOf`/`capEdited`/`capPanelHTML` já eram genéricos (recebem o objeto e o id, nunca a lista); só a escrita e o "Restaurar" amarravam no `INTAKE.cuts`, e viraram `capTarget(id)`, que procura nos dois mundos. CSS novo: zero.
      - **O botão só aparece com o trecho em disco**, junto do botão do Remotion — porque é a resposta do `/api/yt-fetch` que traz as falas. Sem falas não há painel: prometeria uma revisão que não existe. E o `capSeed` semeia da SESSÃO, sem rede.
      - **Trocar a URL derruba a correção** (`capDrop` em cada candidato antigo, dentro do `ytUrlWrite`): sem isso, um id de candidato repetido queimaria o texto de um vídeo em outro.
      - **Sem teto de falas aqui, de propósito:** o trecho é limitado pelo próprio corte. No Passo 3 o servidor **recusa** trecho longo em vez de truncar, e truncar é exatamente o que não se faz — mostrar as primeiras falas como se fossem todas produz override truncado e o resto do clipe sai MUDO.
      - **Armadilhas do próprio teste (não re-descobrir):** o id do candidato é `uid('cand')`, gerado na hora — o teste tem de **ler** o `data-clip` do card, senão o clique cai em "trecho não está mais na lista" e as asserções seguintes passam por engano. `b.aba('youtube')` **estando** na aba sai cedo sem re-renderizar, e o teste passa a ler o HTML velho. E o rótulo "— corrigida" só aparece com o painel FECHADO (aberto ele diz "Fechar legenda").
    - **A legibilidade (✗A) foi medida e NÃO foi consertada — o conserto planejado era placebo.** O plano era "piso de duração por página, com o tempo saindo do silêncio seguinte". Medido na legenda REAL de um podcast de 53 min (`wc3V6vb9Yoc`, 1703 falas): **100% das falas se sobrepõem à seguinte e ZERO empatam o início**. Com 100% de sobreposição o `normalize_cues` fecha cada fala exatamente onde a próxima começa — ou seja **o silêncio entre falas é zero por construção**, e não havia de onde emprestar tempo. Os 57 char/s que motivaram o plano vinham de um caso que **eu construí**, não de dado real.
      - **O que o dado real diz:** mediana da fonte **3,68 s / 37 caracteres** (9,5 char/s, confortável), com as falas espaçadas ~1,86 s (1703 falas em 3170 s). O truncamento **corta o tempo de exibição pela metade**, e 37 caracteres em 1,86 s dão ~**20 char/s** — derivado dos agregados, não medido página por página: a segunda leitura do json3 foi bloqueada por limite de chamadas do YouTube.
      - **A raiz era outra: o `parse_json3` descarta o tempo por PALAVRA (`tOffsetMs`) que o json3 traz**, concatenando os `segs` num texto só. **Isto deixou de ser "o próximo passo" no mesmo dia** — ver o bullet seguinte: o tempo por palavra entrou por `ytclip.parse_json3_words`, função NOVA ao lado, e o `parse_json3` continua byte a byte igual de propósito (o `candidates()` mede a pausa entre falas naquela grade para decidir onde o corte fecha). A amostra json3 real que faltava está em `video-worker/fixtures/json3-rolante.json` — tempos reais, texto sintético.
      - **O empate, consertado no commit anterior, NÃO dispara neste vídeo** (zero empates em 1703 falas). O defeito é real e o conserto está provado, mas a frequência dele em ASR de podcast longo é desconhecida — não inflar.
    - **A linha da legenda passou a durar o tempo das PALAVRAS dela (decisão do usuário, 2026-08-28):** a legenda automática do YouTube é uma **janela rolante** — o evento fica no ar enquanto o seguinte já começou, o `normalize_cues` fecha um onde o outro começa, e o texto falado em 3,68 s aparece em ~1,86 s. O json3 traz o `tOffsetMs` de CADA palavra, que o `parse_json3` jogava fora. Agora **`ytclip.parse_json3_words`** (segunda leitura do MESMO arquivo, zero chamada de rede nova) monta a grade por palavra e **`captions.cues_from_words`** agrupa em linha que começa na primeira palavra e acaba na última — sem sobreposição **por construção**, nada para o `normalize_cues` truncar. A fronteira única continua sendo o `cues_for_range`, então os DOIS renderizadores 9:16 (ASS do FFmpeg e Sequence do Remotion) herdaram juntos.
      - **A recomendação de cortes NÃO mudou, e isso é prova comportamental, não promessa:** o check `17m` roda `candidates()` com e sem `words` e exige saída idêntica. Quem fiar palavra no detector reprova ali.
      - **Sem `words` o caminho é o de antes, item por item** (`17n`): sidecar v1/v2/v3 já gravado, legenda corrigida na mão pelo operador e faixa sem `tOffsetMs` seguem na cue grossa. Legenda MANUAL vira UMA "palavra" por frase e chega inteira ao clipe — nada de picar frase alheia em pedaço de 25 caracteres só porque a função nova existe.
      - **`PAUSA_LINHA_SEC` = 0,7 s é o BOTÃO DE CALIBRAGEM desta entrega** (mora no `captions.py`, ao lado do número medido). Sai do **p90** da amostra de 10857 palavras com instante: dentro da MESMA janela do YouTube as palavras começam a cada ~201 ms (mediana) e a primeira palavra de uma janela vem ~279 ms depois da última da anterior — p10 120 ms, p90 719 ms, **máximo 8500 ms**. No p90 a linha quebra na respiração de verdade e não a cada palavra: **subir junta mais palavra por página** (menos troca na tela), **descer pica a frase**. É também o único número novo de tempo — a tipografia toda (`MAX_CHARS_LINHA`/`MAX_LINHAS`) foi reusada.
      - **O ganho medido é de COERÊNCIA, não de velocidade de leitura:** página que mistura o fim de uma frase com o começo da seguinte caiu de **26 de 61 (43%) para 0** — é isto que o relato "a legenda não completa os assuntos" descreve. A taxa de leitura praticamente não se moveu (**18,8 → 17,8 char/s** mediano; confortável ≈ 15), e não vai: ~18 char/s é a velocidade da FALA e nenhuma paginação a reduz. **O que sobra na mesa** é esticar a linha até o início da seguinte — medido, 23,0 s de intervalo entre linhas num clipe de 125,2 s (18%) que o teto de 0,7 s descarta, e o mediano iria a 16,9 char/s. Não foi feito porque o **mesmo** `PAUSA_LINHA_SEC` faz as duas coisas (quebrar linha E tirar da tela): separar os dois botões é o caminho, e era fora deste pedido.
      - **Armadilha medida — piso EXATAMENTE no limiar é cara-ou-coroa de float, e apagava a última palavra do corte (achada em revisão, consertada em seguida):** `cues_from_words` põe a linha curta no piso de leitura e o `_lines_from_words_in_range` **arredonda para milissegundo DEPOIS**; a regra 1 do `normalize_cues` descarta cue com menos de `MIN_CUE_SEC` **com o texto dentro**. Medido: a palavra aparada em 12,201 → 12,251 dava `0.05000000000000071` no piso e `0.04999999999999893` depois do `round(3)`. **63 das 351 palavras da amostra desapareciam** quando o corte acabava 1 ms depois de a palavra começar — sempre no FIM do trecho, sempre calado. Agora existe **`captions.PISO_LINHA_SEC` = `MIN_CUE_SEC` + 2 ms**: arredondar dois instantes encurta no máximo 1 ms, então 2 ms sobrevivem com 1 ms de sobra. Provado com polaridade nos checks `17u` (o caso medido) e `17v` (as 351 palavras, uma janela por palavra); com o piso de volta no limiar, os dois reprovam. **O `17t`, único check de texto integral, usava UMA janela fixa cuja última linha dura 2,32 s — nunca chegava ao piso, e por isso o defeito convivia com a suíte verde.**
      - **Armadilha de comentário — "a automática do YouTube não pontua" era FALSO** e estava escrito em cima do ramo `FIM_DE_FRASE`, de que a entrega inteira depende. Medido na amostra: **44 das 351 palavras (12,5%) terminam em `.!?`** e outras 29 em vírgula, e a pontuação é **estrutural** (11 das 15 trocas de falante vêm logo depois de palavra pontuada), não invenção do texto sintético. Desligando o ramo, **24 de 53 páginas voltam a partir a frase** — ou seja o ganho de manchete vem TODO dele. Quem lesse o comentário antigo o apagaria como caminho morto.
      - **Sabido e NÃO consertado (fora do pedido, medido como inofensivo hoje):** (a) o caminho por palavra não tem **dedup** nenhum, enquanto o `parse_json3` colapsa linha repetida da ASR — na amostra há **0** pares repetidos e **0** pares com início a menos de 50 ms, então nada duplica; (b) o `cues_from_words` ordena por `(start, text)`, herdado do `normalize_cues`, o que **alfabetiza** duas palavras de início idêntico — na amostra há **0** empates em 10857 palavras, e o `parse_json3_words` já ordena de forma estável.
      - **Trocar a fonte por transcrição LOCAL (Whisper) continua ABERTO — não rodou aqui e não está decidido.** É a saída para o que paginar não resolve: palavra errada, `[ __ ]` e pontuação duvidosa vêm do RECONHECEDOR do YouTube, não do agrupamento — o tempo por palavra só arruma onde a linha quebra. **O custo não é o obstáculo que parece:** o Remotion já cobra ~**11 s de render por 1 s de clipe** medidos nesta máquina, então transcrever os 90 s de áudio de um corte (estimativa grosseira de 12-25 s, **NÃO medida aqui**) seria ruído ao lado disso. O que falta decidir é o resto, e nada disso foi medido: dependência nova num projeto que é stdlib no Python e Vanilla JS sem npm, modelo baixado na máquina, e se a transcrição local ganha mesmo da legenda do YouTube em PT-BR.
  - **A palavra sendo DITA acende (karaokê por palavra no Remotion) — decisão do usuário, 2026-08-31:** o tempo por palavra já existia desde 2026-08-28 (`ytclip.parse_json3_words` → `captions.cues_from_words`), mas morria na fronteira de cue de clipe: o `cues_for_range` devolvia só `{start,end,text}` e o Remotion não tinha como saber QUAL palavra está sendo falada. Agora cada linha carrega `words` e o `Clip.jsx` pinta a palavra corrente de **verde `#59E36A`**, com `spring()` do Remotion levando a escala de 1 a **1,12** e a subida de 0 a **−4px**; fora do intervalo dela, branco e parada. **Só o Remotion** — o ASS do FFmpeg não ganhou animação, e isso é provado (`test_captions` 11f e `test_ytclip` 18h comparam o documento ASS com e sem a chave: byte a byte igual).
    - **A base de tempo é uma só: SEGUNDOS relativos ao começo do CORTE**, e o começo do trecho é subtraído UMA vez, no `ytclip._lines_from_words_in_range`, que já rebaseava a linha. `useCurrentFrame()` DENTRO de uma `<Sequence>` devolve o quadro RELATIVO a ela, então a `Legenda` recebe o prop `de` (quadro inicial da página) e **soma**. Provado com corte começando em 120 s: `test_ytclip` 18f exige que `palavra.start + 120` caia num instante que o json3 realmente publicou, e o 18f2 mostra que subtrair duas vezes ou não subtrair reprovaria.
    - **A palavra ativa vem de função pura (`preset.activeWordIndex`), com intervalo SEMÂNTICO `start <= t < end` — fim EXCLUSIVO.** Isso não é detalhe: no `parse_json3_words` o fim de cada palavra **É** o início da seguinte, então com fim inclusivo DUAS palavras ficariam verdes em todo quadro de fronteira. Sobreposição acidental resolve na última que casa (nunca duas ativas). Lista ausente, tempo ilegível e palavra torta devolvem −1 → legenda estática e branca, o comportamento de antes desta feature.
    - **Alinhar por CONTAGEM de tokens estava errado, e foi MEDIDO:** 18 das 351 palavras da amostra real (5,1%) trazem espaço DENTRO — `>> fulano` (troca de falante do json3) e `[ ca ]` (censura do reconhecedor). Comparando `words.length` com `text.split(' ').length`, essas 18 desalinhavam **16 das 66 linhas (24,2%)** e um quarto da legenda voltava ao estático — alguma linha acendendo e outra não, que parece defeito. `preset.palavrasAlinhadas` confere a **JUNÇÃO** (`words.join(' ') === text`), e aí o átomo da página passa a ser a PALAVRA, não o token: nenhuma linha cai, e `[ ca ]` nunca é partido no meio. Medido: as 66 linhas com tempo por palavra já cabem numa página cada, então esse ramo não reparte nada na prática.
    - **A ênfase semântica NÃO é aplicada quando o karaokê está no ar** — decidido OLHANDO o frame, não por gosto. Numa página `category: money`, o `pickEmphasis` pintava "Faturamento" no verde de dinheiro (`#8FB573`) de forma PERMANENTE ao lado do verde da palavra corrente: duas cores de destaque na mesma tela, que é literalmente o que o comentário do `destaqueGanho` proíbe ("viram semáforo e a hierarquia some"). Ela continua INTEIRA no caminho estático (corte antigo, legenda corrigida na mão, legenda manual) e no ASS. Reverter é voltar o ramo no `Clip.jsx` — mas então uma das duas cores tem de mudar.
    - **Medido no pixel, em frame renderizado de verdade (fonte sintética 16:9, `testsrc2`, 111 quadros):** escala da palavra `empresa.` (260px sem transform) vai a **1,131 no pico (quadro 6 da mola) e assenta em 1,115** — ou seja o 1,12 pedido e o sobressalto de 11,2% que a mola `damping 12 / stiffness 220 / mass 0.5` calcula (ζ = 0,572). **ZERO refluxo, provado por diferença de pixel** contra o quadro do buraco (fonte ESTÁTICA, ninguém aceso): o único retângulo que muda é o da palavra ativa — 32×49px para o `é`, 156×55px para `lucro,` — e todo o resto da legenda é byte a byte idêntico. A quebra de linha e as duas linhas não se movem em nenhum quadro.
    - **A folga até a palavra vizinha é o BOTÃO DE CALIBRAGEM (`TOKENS.palavraEscala`), e ela fica APERTADA:** `transform: scale` cresce o glifo e não a caixa de layout (a armadilha de 2026-08-18, quando 1,05 produziu "Faturamentonão"). **Três casos medidos em frame renderizado, nenhum com sobreposição:** `lucro,` (154px, com vizinha) 21px → 10px; `empresa.` (260px, com vizinha) 20px → **5px assentada e 3px no pico**; `empreendedorismo.` (568px) cresce 37px de cada lado, continua DENTRO da caixa de 820px — e **não tem vizinha**, porque palavra de 17 caracteres num teto de 25 por linha fica sozinha na linha dela. **É isso que salva o layout, e é aritmética, não sorte:** o transbordo cresce com a largura da palavra, e a chance de ela ter vizinha PERTO cai na mesma proporção. **NÃO está provado que nunca sobrepõe:** por conta, uma palavra de ~12 caracteres (~390px) ao lado de uma curta transborda ~25px de cada lado contra ~20px de espaço, ou seja **sobreporia ~5px** — esse caso não foi produzido em frame, é extrapolação. Baixar `palavraEscala` para ~1,08 tira o risco; `1.0` desliga o pop e deixa só a cor. Não foi baixado porque 1,12 é o número pedido.
    - **Nenhuma versão de schema subiu, e isso é proposital:** o sidecar (`.mostreplayed.json` v4) **já** guardava `captions.words` desde 2026-08-28 e não mudou. O que mudou é campo NOVO e OPCIONAL numa cue (`words`), que atravessa `/api/yt-fetch` → `clip.clipCues` → corpo do `/api/remotion-render` → `render_props` → `props-<token>.json` sem ninguém precisar saber dele. Sidecar v1/v2/v3, legenda MANUAL, faixa sem `tOffsetMs`, preset `limpo` e **legenda corrigida na mão** (o `clean_edit_cues` descarta `words` de propósito: o operador reescreveu o texto, a grade antiga descreve outras palavras) seguem na legenda estática. Checks de fronteira: `test_serve` 26r/26s (o valor CHEGA e sobrevive ao JSON) e 26t (a correção perde o tempo por palavra).
    - **O que NÃO foi tocado:** `parse_json3()`, `candidates()` (a recomendação sai idêntica — `test_ytclip` 17m e 18i são prova comportamental, não textual), o caminho FFmpeg/ASS, o portão de direitos, o download, o fundo por miniatura, o teto de render, e a posição/largura/fonte/sombra/2 linhas da legenda. Nenhuma dependência nova, nenhum `createTikTokStyleCaptions`, nenhuma tela de ajuste.
    - **Revisão adversarial (4 lentes independentes, somente-leitura) mudou a entrega em quatro pontos — e o mais grave era invisível:** (1) **o gate do consumidor virou função pura `preset.palavrasDaPagina`.** Escrito à mão dentro do `Clip.jsx`, trocá-lo por `pagina.words` (chave que nunca existiu) **desligava o karaokê em toda página de todo corte** — legenda estática branca, sem erro nenhum — e passava com as 119 verificações verdes. É a MESMA classe de erro que criou o `ancoraLegenda` em 2026-08-27, e o conserto é o mesmo: o teste CHAMA a função com uma página construída pelo próprio `toCaptionPages`. (2) **Página de UM átomo não acende.** Em legenda MANUAL o `parse_json3_words` devolve UMA "palavra" com a FRASE inteira: sem a guarda, a linha toda ficava verde e crescia 12%. Medido: 9 das 66 linhas da amostra automática (13,6%) caem aqui, todas de uma palavra, e saem em branco estático — que é o certo, pela mesma razão do `pickEmphasis` ignorar página de menos de 3 palavras. (3) **Átomo maior que a página derruba o caminho por palavra** e devolve a paginação por token: com a frase manual como átomo, o `|| !atual` deixava passar página de 77 caracteres num teto de 50 e os dois renderizadores divergiam. (4) **O tempo da página passou a sair das PALAVRAS dela**, não da repartição por caractere — a fronteira da Sequence caía num instante que a fala não tem e a palavra acendia atrasada ou não acendia (alcança as 2 de 66 linhas que passam de 50 caracteres). **Onze sabotagens foram exercitadas numa CÓPIA em temporário (nunca na árvore de trabalho — a lição de 2026-08-26) e todas reprovam**, cada uma num check nomeado.
    - **A revisão não terminou: 12 dos 13 céticos morreram no limite de gasto.** Só um achado foi julgado (o do gate, confirmado e reproduzido). Os outros 12 estão **não verificados**, não refutados — e a triagem à mão contra o artefato derrubou vários: a ênfase na palavra errada já não existia (o ramo saiu), o "46% das palavras nunca completam a mola" mediu **22,5%** de palavras abaixo de 4 quadros (a mola pica no quadro 6, e a mediana da palavra é 6,0 quadros), e a sobreposição de tinta não se reproduziu em nenhum dos três frames medidos. **Continua em aberto e não conferido:** o `durationInFrames` da página (`round(start·fps) + round((end−start)·fps)` pode passar do `from` da página seguinte e pôr duas páginas no ar por um quadro) — **defeito PRÉ-EXISTENTE, do código original que este pedido não tocou, e que afeta o caminho estático igual**.
    - **Sabido e NÃO consertado (fora do pedido):** (a) a **volta** ao branco é instantânea, sem mola — a palavra seguinte acende no mesmo quadro, então a troca some; animar a saída exigiria guardar por palavra o quadro em que ela apagou. (b) A legenda **corrigida na mão** perde o karaokê inteiro, mesmo quando o operador só arrumou uma letra — realinhar palavra corrigida com a grade antiga é um problema de alinhamento de texto, não de tempo. (c) `pickEmphasis`/`splitEmphasis` já não são chamados no ramo do karaokê, então a ênfase semântica **não existe** em corte novo de YouTube com legenda automática — na prática ela virou recurso do caminho estático.

  - **O marcador do reconhecedor saiu da legenda, e o buraco continuou buraco (decisão do usuário, 2026-08-31):** relatado como `>> Tá bom.` e `[__]` queimados no 9:16. Não são fala de ninguém — `>>` é a troca de falante do json3 e `[ __ ]` é a censura de palavrão do reconhecedor. **Um ponto só:** `captions.strip_artifacts` (puro, idempotente) chamado de dentro do `captions.normalize_cues`, que é o gargalo por onde TUDO passa — ASS do FFmpeg (via `to_pages`), Sequence do Remotion e painel de revisão do Passo 3 chegam todos pelo `ytclip.cues_for_range`, que termina nele. Nenhum renderizador ganhou limpeza própria, então nenhum pode divergir: é a mesma lição do `margem_inferior`, que já é dono único da POSIÇÃO.
    - **Conjunto FECHADO de dois marcadores** (`>>+` e `\[\s*_+\s*\]`), e fechado de propósito: tudo que não for provadamente artefato é fala de alguém. **`[Música]`, `[Aplausos]`, reticência, aspas e `3 > 2` ficam** — medido em 46 entradas, os únicos casamentos indevidos são texto que traz `>>` literal (`a >> b` de deslocamento de bit, o prompt `>>>` do Python), que num podcast de negócios em PT-BR não é o caso. A substituição é por **ESPAÇO, nunca por vazio**: com vazio, `a>[__]>b` viraria `a>>b` e a função deixaria de ser idempotente.
    - **Segmento que era só artefato NÃO vira legenda — mas o instante dele continua valendo.** É a parte que não é óbvia: a legenda automática é ROLANTE (a fala de trás invade a seguinte), então se a cue vazia fosse simplesmente descartada, quem apararia a anterior passaria a ser a PRÓXIMA fala de verdade e ela **cobriria o buraco inteiro**. A cue vazia sobrevive como **barreira**: apara o fim da anterior no próprio início e é jogada fora antes de sair. Buraco curto vale mais que fala esticada. Medido: `Eu comecei` para em 11,2 (o começo do artefato) em vez de escorrer até 11,8.
    - **A barreira nunca custa fala de verdade:** se aparar deixasse a anterior curta demais para ser lida, ela é ignorada e a próxima fala apara como sempre aparou. Note que a barreira **não** tem a rede da regra 5 (fundir na seguinte) — fundir ali empurraria fala real para dentro do buraco, que é exatamente o proibido.
    - **Armadilha medida — `a + PISO` e `b - a >= PISO` são perguntas DIFERENTES, e a diferença apagava a frase inteira (achada em revisão adversarial, consertada):** a guarda da barreira nasceu como `cue["start"] >= saida[-1]["start"] + MIN_CUE_SEC` enquanto a regra 1 cobra `fim - inicio < MIN_CUE_SEC`. Com gap de **exatamente 50 ms** as duas discordam por um ULP: `4.05 >= 4.0 + 0.05` é True, mas `4.05 - 4.0` dá `0.04999999999999982`. Como o caminho do FFmpeg normaliza **DUAS vezes** (`cues_for_range` e depois `to_pages`), o primeiro passe aceitava a cue aparada e o segundo a **descartava com o texto dentro** — `eu quebrei duas vezes antes de acertar` sumia do ASS, calado. É a mesma família da armadilha que criou o `PISO_LINHA_SEC`; aqui não há `round` no meio, então a **expressão idêntica** basta e não precisa de folga inventada. Conferido com controle: trocando o artefato por texto que a regra não limpa, a fala sobrevivia — ou seja o defeito era do ramo novo, não pré-existente.
    - **As PALAVRAS passam pela mesma regra**, senão o karaokê acenderia o marcador: o Remotion só pinta palavra a palavra quando `words.join(' ')` bate com o texto da página (`preset.palavrasAlinhadas`), e limpar só um dos dois derrubaria todo corte para a legenda estática. Palavra que era só artefato sai da lista; **o instante das vizinhas não é tocado**.
    - **`render_props` entrou no gargalo (o último renderizador fora dele).** O props do Remotion copiava `body["cues"]` **verbatim** — hoje não vazava artefato, mas por PROVENIÊNCIA (os dois alimentadores já vêm limpos), não por construção. A assimetria era real: a MESMA correção digitada no painel atravessava `clean_edit_cues` → `normalize_cues` indo para o FFmpeg e **nada** indo para o Remotion, então duas falas sobrepostas saíam empilhadas num renderizador e aparadas no outro.
    - **O `parse_json3` NÃO foi tocado, de propósito** — o `candidates()` mede a pausa entre falas naquela grade para decidir onde o corte fecha, e a recomendação está fora deste pedido. O check `19k` prova que a restrição é REAL e não decorativa: limpar a entrada do detector **mudaria** a saída (medido, 15 das 60 cues perdem o marcador). **Aqui morava uma tautologia:** ele comparava `candidates(info_rec)` com `candidates(dict(info_rec, cues=list(grossas)))`, e `info_rec["cues"]` **já é** `grossas` — os dois lados eram a mesma entrada pelo mesmo código, e o check passava até com a limpeza aplicada dentro do `parse_json3`.
    - **Sabido e NÃO consertado (medido, fora deste pedido):** (a) como o detector lê texto cru, o **`hook`/`topic` do candidato sai com o marcador** — e ele não é só entrada de cálculo: aparece no cartão de trecho, vira o **nome do clip na Central** (persistido em `pp_video_clips_v1`), entra no **nome do MP4** e vai na resposta pública da Fase 1 (`cloud/probe_server.CANDIDATE_FIELDS`); o conserto é limpar o campo de SAÍDA em `ytclip.py:719`, sem tocar no cálculo. (b) Artefato como **primeira palavra** de uma linha: a linha guarda o instante dela, então o texto real aparece ~685 ms antes de ser falado (1 de 62 linhas da amostra) — nenhum instante MUDOU e nenhum artefato é exibido, só o buraco não sai nesse caso fino. (c) Artefato partido em segs separados (`[`, `__`, `]`) casa no texto concatenado e não em átomo nenhum, então a linha perde o karaokê e cai para estática — **não observado em dado real**. (d) `_resolve_empate` pode devolver fatia que ultrapassa o grupo seguinte, quebrando a idempotência — **pré-existente, reproduzido com zero artefatos**.
    - **Revisão adversarial (3 lentes somente-leitura + 1 de sabotagem em CÓPIA no temporário) mudou a entrega em quatro pontos:** o defeito do ULP acima (achado por duas lentes independentes, com números batendo); os checks `12n2`/`12n3`/`12x`/`12y`/`12z`, porque **a sabotagem S6 — tirar o piso da guarda — passava com as 149 verificações verdes**: o `12n` comparava só o TEXTO, e sem o piso a fala sobrevive como **piscada de 10 ms** no ASS, ilegível e calada (texto e duração são coisas diferentes, e a razão escrita no código fala de LEITURA); a tautologia do `19k`; e o `render_props`. Das dez sabotagens exercitadas, nove já reprovavam num check nomeado. **Verificado depois do conserto: S6 agora reprova em 5 checks.**
    - Checks — **rode `.\provas.ps1`**: `test_captions.py` (154) · `test_serve.py` (**287**) · `test_ytclip.py` (194) · `test_worker.py` (**116**) · `test_helper.py` (130) · `studio/test-preset.mjs` (**322**) · `test-video-ops.js` (**72**) · `test-video-ops-dom.js` (**108**) — **1383 verificações nas oito, zero falhas**.

  - **Destaque no TÍTULO do card, por PESO e nunca por cor (decisão do usuário, 2026-09-01):** o título era uma frase com um peso só — toda palavra pesando igual, então nenhuma pesando, e o número que faz o vídeo valer o clique sem hierarquia nenhuma. Agora `preset.pickTitleHighlight` escolhe UM trecho (`100 mil pedidos`, `R$ 2 milhões`, `40%`) e o `Titulo` do `Clip.jsx` o sobe de Montserrat 800 para 900 com um filete embaixo. Executa o `docs/PLANO-destaque-titulo.md`.
    - **Sem IA e sem dependência nova, de propósito:** o pedido do card proíbe "AI or NLP dependency" e "unreliable automatic keyword detection" por escrito. É léxico fechado + regra de token, mesma classe do `pickEmphasis` que roda desde 2026-08-18 — não adivinha, erra sempre igual, e o conserto é editar a lista. O `highlightText` do operador **vence sempre**, e `autoHighlight:false` desliga o automático inteiro.
    - **Trecho, não palavra, e devolvido em ÍNDICE de token.** Em `"Gastei 50 mil e faturei 300 mil"` um `replace('mil')` pinta o PRIMEIRO, e o certo é o segundo. Cresce à esquerda pela moeda (`R$`) e à direita por `ESCALA` e depois UMA `UNIDADE_RESULTADO`, com teto de 4 tokens, e **nunca atravessa vírgula** — destaque partido em duas orações não se lê como uma coisa só.
    - **Lista fechada de unidade e não "qualquer substantivo": FALHA PARA O LADO SEGURO.** Sem ela, `"Faturei R$ 2 milhões vendendo capinha"` engoliria `vendendo`, e separar verbo de substantivo exige morfologia — exatamente a dependência proibida. Unidade desconhecida faz o trecho parar cedo e sair `R$ 2 milhões`, que já está certo.
    - **Quando NÃO destacar é METADE do recurso:** menos de 5 palavras (a frase já é o destaque inteiro), trecho cobrindo mais de 45% dos caracteres (se tudo brilha, nada brilha), número nu (`as 3 coisas` não fica mais claro com o `3` forte) e ano precedido de `em`/`desde`/`até` (`em 2019` não é resultado — mas `cheguei a 2000 clientes` continua valendo). `null` é desfecho legítimo, não falha.
    - **`FRACOS_NO_TITULO` é FILTRO, não segunda lista.** `TERMOS_FORTES` é compartilhado com a legenda e traz intensificadores (`nunca`, `sempre`, `ninguem`, `tudo`, `nada`): numa PÁGINA de legenda "nunca" é a palavra que muda o sentido, num TÍTULO ele não é a afirmação — destacá-lo é o "highlight random words to create movement" que o pedido proíbe. Duas listas divergiriam e o mesmo termo passaria a valer num renderizador e não no outro.
    - **Cor foi descartada por medição, não por gosto:** a marca do canal é monocromática (0 pixels cromáticos no logo — ver `logo-ecommerce-puro/`), e usar o amarelo/verde da legenda no título poria duas linguagens de destaque no mesmo quadro. **Montserrat 900 teve de entrar no `loadFont`** — sem o arquivo o navegador SINTETIZA o peso a partir do 800, e sai um engrossamento borrado que só aparece OLHANDO o frame. 800 contra 900 sozinho é sutil demais a 50px, daí o filete: que é a régua ladeando o `PURO` no logo, device que a marca já tem, não enfeite novo.
    - **Verificação visual FEITA (fonte sintética `testsrc2`, quatro stills).** O trecho sai mais pesado e sublinhado; título SEM destaque renderiza **idêntico** ao de antes (`tituloPeso` continua 800); acento e vírgula decimal (`R$ 1,2 milhão`) sobrevivem. **Medido: o trecho PODE quebrar entre duas linhas** (`100` / `mil pedidos`) e renderiza certo, porque `text-decoration` sublinha cada fragmento — retângulo posicionado não faria isso. **`nowrap` NÃO foi forçado de propósito:** com o teto de 4 tokens ele quase sempre caberia, mas um caso patológico produziria overflow horizontal, proibido por escrito no pedido. Split legível vale mais que overflow.
    - **11 sabotagens exercitadas numa CÓPIA em temporário** (nunca na árvore de trabalho — a lição de 2026-08-26) e **todas reprovam num check nomeado**: empate invertido, cada uma das quatro guardas, `highlightText` deixando de vencer, span virando string, o filtro de intensificador removido, o gate trocado por valor à mão, o 900 saindo do `loadFont` e o span não chegando ao `Titulo`. As guardas são provadas por PARES que discriminam (`Cortei 40% do custo mensal` destaca, `Cortei 40% do custo` não) — um caso só passaria verde com a guarda removida.
    - **`render_props` do `serve.py` NÃO mudou:** `highlightText`/`autoHighlight` entram em `defaultProps`, então prop ausente cai no default e todo render antigo sai igual. O caminho FFmpeg/ASS não tem título e não foi tocado.
    - Checks: `.\provas.ps1` (o `studio/test-preset.mjs` foi de 133 para **166**).

  - **A miniatura passou a entrar no tamanho da TARJA, duas vezes, em vez de uma esticada cobrindo o quadro (decisão do usuário, 2026-09-01):** relatado olhando um corte real, com as duas tarjas marcadas em vermelho. O fundo por miniatura (2026-08-26) escalava UMA imagem em `cover` sobre os 1080x1920 — o que **amplia a miniatura ~3,2× e recorta 68% da largura dela**, então cada tarja mostrava uma FATIA gigante e sem sentido: no caso relatado, o título da miniatura aparecia em letras enormes, cortado no meio. Agora ela é escalada para 1080×`banda` e desenhada **inteira, duas vezes** — uma no topo, uma na base.
    - **Um dono só para a conta: `worker.band_height(video_h)`** = `(1920 − altura_do_vídeo) // 2`, 656 para um 16:9. O filtro do FFmpeg recebe `band_h` e o Remotion recebe o prop `bandaAltura` — os dois saem da MESMA função. Duas contas independentes fariam o mesmo corte enquadrar a miniatura em lugares diferentes em cada renderizador, calado; é a disciplina que o `captions.margem_inferior` já tem para a âncora da legenda.
    - **Custo medido e aceito: 43px de cada lado.** A tarja é 1080×656 (proporção 1,65) e a miniatura é 16:9 (1,78), então `cover` corta **4% da largura** — visto no frame de prova, onde o canto da miniatura sintética virou "anto". É o preço de não deixar faixa vazia entre a miniatura e o vídeo, que foi o que o pedido descreveu ("no tamanho da parte demarcada"). Para preservar a miniatura inteira e aceitar a faixa: `increase`+`crop` viram `decrease`+`pad` no worker, e `cover` vira `contain` no `Fundo`.
    - **ZERO é resposta legítima, e por isso `ancoraBanda` NÃO usa `Number(v) || padrão`.** Fonte já vertical enche o quadro e não sobra tarja: aí não se desenha nada, em vez de esconder miniatura atrás do vídeo. O `||` do `ancoraLegenda` trocaria esse 0 por 656. E `null`/`''` são tratados como AUSENTE antes do `Number()`, porque `Number(null)` é 0 — prop faltando apagaria o fundo. Mesma família do `if override is not None` do serve.py.
    - **Sem `band_h` o filtro é o de antes, byte a byte** (check 20m/20m2): MP4 local do Passo 3 e trecho em cache antigo, que não têm dimensão medida, seguem no comportamento anterior. Nada de render antigo muda de aparência de tabela.
    - **A fiação `serve → worker` tem check próprio (20q), interceptando `render_cut`.** Sem ele, apagar o argumento na chamada de `render_to` mataria a tarja em todo download do Passo 3 com as oito suítes verdes — os outros checks só provam que o `build_filter` SABE fazer, não que alguém pede. Mesma lição do "`in arquivo` só prova que alguém escreveu a palavra".
    - **Verificação visual FEITA nos DOIS renderizadores** (fonte sintética `testsrc2` + miniatura sintética com texto e borda): Remotion e FFmpeg saem com o mesmo enquadramento, o mesmo recorte de 43px e o mesmo escurecimento; `banda=0` não desenha nada. O check pré-existente `6n` fixava a lista exata de props do `Fundo` e teve de ser atualizado — a intenção dele (nenhum caminho de vídeo entra no `Fundo`, senão volta o decode duplo de 2026-08-27) foi preservada, junto do `6m`, que CONTA as tags `<Video>`.
    - Checks: `.\provas.ps1` (`test_serve.py` 202 → **219**, `studio/test-preset.mjs` 166 → **177**).

  - **O título virou CARD da marca — e antes disso ele não aparecia em render nenhum (decisão do usuário, 2026-09-01):** relatado como *"mesmo já tendo, ainda não vê quando está sendo editado no Remotion junto do vídeo"*. **Não era a composição: era `title: ''` fixo no `video-ops.js`**, na única chamada de `/api/remotion-render` que existe. A composição só monta o título com `title` preenchido, então o destaque inteiro entregue horas antes — `pickTitleHighlight`, `resolveTitleHighlight`, peso 900, filete, **33 verificações verdes** — existia, era testado e **nunca chegou à tela**, nem no render nem no Remotion Studio (cujo `defaultProps.title` também era `""`). É a família do **BP-014**: a suíte cobria a função pura e ninguém cobria a FIAÇÃO.
    - **O conserto da raiz é uma função, não uma linha.** O corpo do POST virou **`renderBody(clip, comLegenda)`**, pura e exportada, e o teste a CHAMA com um trecho construído. Enquanto o corpo era montado inline dentro do `fetch`, a única prova possível era regex — e foi exatamente por ali que o `''` passou. O título é o **`clip.topic`**, que já nomeia o arquivo baixado e o cartão na Central: a manchete é o que o operador já lê na tela, não um campo novo. `defaultProps.title` passou a vir preenchido, para o Studio abrir MOSTRANDO o card (prop mandado vence defaultProp, então não vaza para render nenhum).
    - **Não existia card branco.** O pedido dizia "substituir o retângulo branco arredondado com texto preto"; o que havia era um `<span>` branco solto sobre o vídeo (Montserrat 800, 50px, centrado, sombra de leitura). O trabalho foi **construir** o card, não substituí-lo.
    - **MONOCROMÁTICO por medição, não por gosto.** O pedido mandava destacar "com a cor de destaque primária do logo" e traçar um filete "na mesma cor da marca". O `logo-ecommerce-puro/README.md` mediu o logo de referência: **0 pixels cromáticos** — "não existe cor de destaque para extrair dela, e inventar uma seria inventar a marca". Levado ao usuário como pergunta antes de codar; ele escolheu **manter monocromático**. A hierarquia é peso (800 → 900), tamanho (+8%) e filete branco — a régua que já ladeia o `PURO` no logo.
    - **A placa é o `badge`, e isso não foi escolha minha:** o README da identidade já registrava que "o card precisa da placa **sem** o `CT`" e que **62px é o tamanho verificado neste card 1080x1920". Ela vale por logo E por identificador do canal — inventar um `@handle` ao lado violaria "não invente um nome de canal novo". Se um dia quiser o handle, é uma linha.
    - **A marca viaja EMBUTIDA (`studio/src/marca.js`, data URI gerado do SVG).** O `--public-dir` do render aponta para a pasta de **cache do YouTube**, então `staticFile()` **não alcança o repositório** — mesma parede que a fonte Inter já tinha batido, mesma solução. Os SVGs estão em curvas: não dependem de fonte instalada em máquina nenhuma.
    - **Tamanho do título é escada determinística, não fórmula:** `TITULO_FONTES` = [58, 52, 46, 41, 36, 32], desce até caber em **3 linhas**, quebra sempre por PALAVRA, e só apara (fronteira de palavra + reticência) quando nem o piso cabe — com `topic` limitado a 140 chars, o piso cobre o caso normal. **`AVANCO_MONTSERRAT` (0.58) é o BOTÃO DE CALIBRAGEM** e é estimativa de métrica de fonte: mexeu, rode `studio/preview-titulo.mjs` e OLHE.
    - **Os tokens do destaque pesam mais na medida** (`tituloDestaqueFator`), porque saem renderizados maiores. Medir como se fossem normais é como a terceira linha vira quarta calada. E o destaque cresce por **`fontSize`, nunca `transform: scale`** — a armadilha do "Faturamentonão" (scale cresce o glifo e não a caixa).
    - **Armadilha medida OLHANDO O FRAME — px fixo não desce com o corpo:** `tituloFileteFolga: 10` e `tituloFilete: 4` foram afinados quando o título tinha UM tamanho. Com a escada descendo a 32px, o sublinhado de `disciplina` encostava nos acentos da linha de baixo (**folga de 0,7px**). O sublinhado passou a `em` (`filete / tituloFileteRef`); o filete da ESQUERDA continua px, porque é cromo do card e não texto. A entrelinha virou token (`tituloEntrelinha` 1.16 → 1.22): 1.16 era apertado para três linhas. **Nenhum teste pegaria isso** — é métrica de fonte.
    - **Armadilha do próprio teste visual:** o caso "quadro claro" nasceu errado. Com fonte 16:9 o card cai **inteiro dentro da tarja de cima** e nunca encosta no vídeo, então testar contraste ali não prova nada. Só fonte **já vertical** (que enche o quadro) põe branco atrás do card. Corolário útil: no caso normal (podcast 16:9) **o card não cobre o rosto de ninguém por construção**.
    - **Verificação visual FEITA** (fonte sintética `testsrc2`, 8 stills 1080x1920): título curto, o do pedido, longo de 106 chars, acentos/números/pontuação, destaque manual, sem destaque, sobre quadro branco e um quadro no meio da entrada. Confirmado olhando: 3 linhas no máximo, nada cortado, nada estourando, o trecho destacado **atravessa quebra de linha e o sublinhado acompanha** (é `text-decoration`, não retângulo posicionado), `R$ 1,2 milhão` inteiro e **`2019`/`2021` recusados** pela guarda de ano.
    - **Entrada contida:** 9 quadros (300ms), `interpolate` com `Easing.bezier(.23,1,.32,1)` grampeado dos dois lados — **não `spring`**, porque mola tem sobressalto e o pedido proíbe quicar. Sobe 12px, abre de 0.98 (**nunca de `scale(0)`**), e depois fica PARADO. Só `opacity` e `transform` animam. A polaridade mora em `entradaCard`, função pura — inverter a subida faz o card DESCER para o lugar e passa em qualquer asserção de texto.
    - **8 sabotagens exercitadas numa CÓPIA em temporário** (nunca na árvore de trabalho — a lição de 2026-08-26) e **todas reprovam num check nomeado**: o `title: ''` original, o peso do destaque removido, a polaridade da entrada invertida, o corpo medido não chegando ao card, o portão voltando ao título cru, o teto de 3 linhas ignorado, a proporção da placa chutada, e a limpeza do título removida do `serve.py` (esta última com polaridade conferida). **O `strip_artifacts` no título é regra ÚNICA compartilhada com a legenda:** o `topic` nasce da cue CRUA (o detector precisa dela assim), então chegava com `>>` e `[ __ ]` — queimados numa MANCHETE, ainda mais à vista.
    - **O que NÃO foi tocado:** legenda (posição, tempo, karaokê, revisão), detecção e recomendação de cortes, download, portão de direitos, fundo por miniatura, tarja, enquadramento, teto de render, caminho FFmpeg/ASS (que não tem título) e as telas do Estúdio. `render_props` não ganhou chave nenhuma — `highlightText`/`autoHighlight` vivem em `defaultProps`, então todo render antigo sai igual.
    - **Sabido e NÃO consertado (fora do pedido):** (a) o operador não tem onde **digitar** a manchete — ela é sempre o `topic`; o `highlightText` existe como prop mas nenhuma tela o preenche. (b) O card fica em `cardTopo: 150` fixo; "posição ajustável" é o token, não uma tela. (c) Título acima de ~117 caracteres é aparado com reticência — inevitável com teto de 3 linhas e piso de corpo legível.
    - Checks: `.\provas.ps1` (`test_serve.py` 219 → **222**, `studio/test-preset.mjs` 177 → **232**, `test-video-ops.js` 41 → **45**). Stills: `cd studio && node preview-titulo.mjs`.
    - **O card virou CHAMADA de 4s no meio do vídeo, e o título ficou editável (mesmo dia):** três pedidos numa tacada — posição, duração e edição.
      - **Vive numa `<Sequence>` de 4s**, não montado o clipe inteiro: entra (9 quadros), fica parado, sai (6 quadros) e **some**. Ele é miniatura e chamariz; passados os 4s só tamparia o vídeo, e fora da Sequence não sobra elemento para custar quadro. Clipe mais curto encurta a janela junto (`Math.min` com `durationInFrames`) — Sequence maior que a composição derruba o render.
      - **Saída mais curta que a entrada** (200ms contra 300ms), e é regra: o espectador está DECIDINDO quando ela entra e o sistema está só RESPONDENDO quando ela sai. `presencaCard(quadro, total)` é pura e é onde mora a **polaridade da saída** — trocar `total - q` por `q` deixaria o card invisível por 3,8s dos 4s, calado.
      - **Armadilha medida OLHANDO O FRAME — "no meio" exato colide com a legenda:** em `cardCentroPct: 0.50` a base do card cai em **y 1158** e uma página de legenda de 2 linhas começa em **y 1079**; a fala era desenhada POR CIMA do card. Legível, com cara de defeito. Ficou **0.44**, e a folga virou **aritmética no check 12d2** (pior caso dos dois lados) em vez de número escolhido no olho — descer daqui reprova.
      - **Título editável no cartão do trecho** (`data-clip-field="topic"` → `clipFieldWrite`), sem re-render, irmão do `cutFieldWrite` (BP-001: reescrever a lista mata o foco). O `topic` do detector vira **ponto de partida, não manchete final**. Um campo alimenta **três lugares** de propósito — o card, o nome do arquivo e o cartão da Central; dois campos divergiriam e ninguém saberia qual manda. **Apagar tudo é resposta legítima:** título vazio = sem card.
      - Sabotagens conferidas: saída invertida · sem Sequence · janela não chegando ao componente · card de volta a 0.50 · campo que não grava. **Todas reprovam num check nomeado.**
      - Checks: `.\provas.ps1` (`studio/test-preset.mjs` 232 → **252**, `test-video-ops.js` 45 → **49**).
    - **A marca do card virou PRIMO RICO, preto e laranja (decisão do usuário, 2026-09-01, mesmo dia):** reverte o "monocromático" registrado acima. A marca anterior (`logo-ecommerce-puro/`) não tinha cor para extrair; esta tem, então o destaque do título **deixou de ser peso e virou COR**, que era o que o pedido original queria desde o começo.
      - **A paleta foi MEDIDA, não escolhida:** dos 50.416 pixels da referência, `#000000` são **82%** e `#FF5F01` é a cor cromática dominante (798px). Os laranjas escuros da contagem (`#AE3B03`, `#B54303`) são **halo de compressão**, não tinta — quem pegar o mais frequente sem olhar a saturação escolhe errado. Laranja de saturação ~1,0 mas **luminância 0,50**: é isso que o faz ler como financeiro em vez de aviso, e é por isso que ele passa no check de neon.
      - **O emblema saiu de uma referência de 184×274 (capa de livro) e ainda assim serve — porque é REDUÇÃO:** 110px de origem para 62px no card. Capa, lombada e selo da editora ficaram de fora. `logo-primo-rico/emblema.png` é o asset regerável; o `marca.js` é gerado dele. **Se aparecer o vetorial oficial, troque o PNG e regere.**
      - **Armadilha medida — máscara por luminância nasce com o alfa pela METADE.** O emblema é laranja puro sobre preto puro, então o brilho É a máscara — mas a luminância ITU-R 601 do `#FF5F01` é **132 de 255**, e o emblema saiu a **52% de opacidade**: visivelmente apagado ao lado do título, e só aparece OLHANDO O FRAME (nenhum teste vê opacidade). O alfa é escalado por `255/L` (fator 1,93), o que põe a tinta em força cheia e **preserva o degradê das bordas**.
      - **Montserrat SAIU do projeto.** Ela existia para o wordmark da marca anterior; o título passou a **Inter Black (900)**, peso que a legenda já carregava. Nenhuma fonte nova entrou e um arquivo a menos é carregado antes do primeiro quadro. O check `9w` cobra as duas pontas: Inter 900 presente **e** nenhum vestígio de Montserrat.
      - **O destaque perdeu o sublinhado e o peso extra, de propósito:** com a cor carregando a ênfase, somar peso + filete seria dizer a mesma coisa três vezes. `tituloPesoDestaque` e `tituloFileteRef` saíram; `tituloFilete` ficou, porque agora é só o filete laranja da esquerda do card. O identificador (`PRIMO RICO | CORTES`) é branco a 72%, **não laranja**: emblema, borda, filete e destaque já são laranja, e um quinto faria o card competir consigo mesmo.
      - **`OFICIAL` não entra** — o pedido proíbe a palavra sem autorização declarada e o projeto não a define. Fixado pelo check `11z9b`.
      - **Atribuição é decisão do operador, não do código:** o card passa a assinar o vídeo com a marca de um terceiro. O portão de direitos do Estúdio (declaração por URL) continua sendo o único lugar onde isso é afirmado — o card não verifica nada.
      - Checks: `.\provas.ps1` (`studio/test-preset.mjs` 252 → **257**).
    - **As DUAS marcas passaram a COEXISTIR, escolhidas pelo operador por trecho (decisão do usuário, 2026-09-02):** a troca para Primo Rico (no mesmo dia da entrega anterior) **apagou** o card monocromático do Ecommerce Puro — o operador ficou sem escolha. Agora há um seletor **`Card visual`** em cada cartão de trecho, e a escolha atravessa o caminho inteiro: tela → estado do trecho → corpo do POST → `render_props` → `props-<token>.json` → composição.
      - **O campo canônico é `titleCardStyle: 'primo_rico' | 'puro_ecommerce'`**, conjunto FECHADO com validador em CADA camada (`preset.titleCardStyleOf`, `video-ops.titleCardStyleOf`, `serve.title_card_style`). **O rótulo da tela nunca é a chave** — "Puro Ecommerce" cai no padrão, como qualquer valor torto. **Ausente = `primo_rico`**, porque é o que todo corte já renderiza: trocar a marca de vídeo antigo por causa de um refactor não é papel deste pedido. **Provado no PIXEL, não asserido:** o still de um clipe sem a chave é **byte a byte idêntico** ao de `primo_rico` explícito, e o de um valor inválido também.
      - **O design do Ecommerce Puro NÃO foi inventado — foi reconstruído do que o repositório já declarava.** O código dele nunca chegou a existir no git (todo o card é trabalho não commitado; o índice e a árvore trazem as duas versões do Primo Rico). O que existe é a especificação: o `logo-ecommerce-puro/README.md` (tokens `#000000`/`#FFFFFF`, "**0 pixels cromáticos**", o `badge` como "a peça que entra no card 1080x1920", **62px verificado**) e este CLAUDE.md (hierarquia "peso 800 → 900, corpo +8%, filete branco"). **Asset: `badge-fundo-escuro.svg`** — e a variante é regra escrita, não gosto: `fundo-escuro` é a de usar SOBRE fundo escuro (placa branca), e o README proíbe a `fundo-claro` ali por escrito.
      - **Nenhuma FAMÍLIA de fonte nova, e a Montserrat continua fora.** A implementação antiga do card monocromático usava Montserrat no título; o README da identidade manda o contrário — "O texto do card continua em **Inter**. Montserrat só na marca" — e as duas placas estão em CURVAS (PNG e SVG), então nenhuma depende de fonte instalada. Entrou **um peso** (Inter 800), porque no monocromático o destaque é 800 → 900. Sem o arquivo do 800 o navegador SINTETIZA a partir do 700 e sai borrado — só aparece olhando o frame, mesma armadilha que o 900 já teve.
      - **O ALGORITMO de destaque é UM (o pedido proíbe duplicá-lo).** `pickTitleHighlight`/`resolveTitleHighlight`/`splitTitleHighlight`, a escada de corpo, a quebra por palavra, o teto de 3 linhas, o aparo e a janela de 4s ficaram compartilhados; o preset possui só a IDENTIDADE (placa, identificador, cor de borda, cor de filete, peso e aparência do destaque). Prova comportamental: o check `13r` roda o gate para as duas marcas sobre cinco manchetes e exige o MESMO trecho.
      - **A GEOMETRIA é compartilhada de propósito, e isso tem declarante.** `larguraTitulo()` lê `TOKENS` — um valor para as duas —, e nenhum preset o recebe. Está certo hoje (mesmo tamanho, respiro e filete; 62px verificado nas duas). O `TITULO_GEOMETRIA_COMPARTILHADA` + check `13t` **reprovam** quem der padding ou filete próprio a uma marca, com o recado de passar o preset ao `larguraTitulo`/`tituloEscalonado` — senão a estimativa de linhas mente PARA MAIS e o estouro só aparece no frame.
      - **Três tokens globais foram REMOVIDOS, não deixados como padrão:** `tituloPeso`, `tituloDestaqueCor` e `cardBorda`. Um valor global não consegue ser laranja numa marca e peso na outra, e token que nada lê é a armadilha clássica (alguém o ajusta e o valor que manda está noutro lugar). O `cardBordaPeso` FICOU — é geometria, e o `larguraTitulo` o desconta.
      - **O seletor é RADIO nativo**, não botão com classe trocada no clique: o `:checked` desenha o selecionado, o browser dá navegação por seta e o `fieldset`/`legend` nomeia o grupo — zero JS de estado visual, que é justamente o que dessincroniza do dado. `name` leva o id do trecho (um `name` só faria os cartões brigarem pelo mesmo grupo). Não re-renderiza (BP-001), e **persiste**: `YT.candidates` E `project.candidates` são os MESMOS objetos, então `projectsPersist()` no clique faz a escolha voltar com o projeto.
      - **Defeito ACHADO e consertado de tabela:** `projectsPersist()` não tinha guarda e o handler novo pode rodar antes do `init` — gravar `PROJECTS` nulo **APAGARIA os projetos salvos**. Agora `if (!PROJECTS) return false;`.
      - **Verificação visual FEITA** (fonte sintética `testsrc2`, mesmo título, mesmo destaque, mesma miniatura, mesmo quadro 60): confirmado OLHANDO que o Primo Rico sai idêntico ao de antes; que o Ecommerce Puro sai com a placa branca, **sem identificador de texto** (a placa já traz o wordmark — inventar um `@handle` ao lado violaria a regra da marca), filete e borda brancos, destaque por peso + sublinhado; que **o sublinhado atravessa a quebra de linha** e acompanha os dois fragmentos (é `text-decoration`, não retângulo posicionado); e que **não há um pixel de laranja** no monocromático. Stills: `cd studio && node preview-titulo.mjs` (casos 12, 13 e 14).
      - **Fora deste pedido, de propósito:** o caminho FFmpeg/ASS (que não tem título e não foi tocado), a tela do Passo 3 (o seletor vive no cartão do trecho do YouTube, que é o único caminho com card), e um campo para o operador DIGITAR manchete — ela continua sendo o `topic`.
      - **Sabido e NÃO consertado:** o `topic` editado no campo continua **não** sendo persistido (é por tecla, e gravar a cada letra martelaria o `localStorage`) — a escolha do card persiste porque é um clique. Assimetria real, herdada, e o conserto é a mesma linha.
      - **ARMADILHA MEDIDA — radio escondido com `position: absolute` e SEM ancestral posicionado rola o contêiner de overflow OCULTO e "quebra a tela" (relatado pelo usuário, consertado 2026-09-02):** trocar a identidade do card deixava a página numa faixa de ~200px, sem barra de rolagem e sem como voltar. **Não era o layout do seletor** — provado numa página isolada com o CSS real: altura do documento, do cartão e do fieldset idênticas antes e depois do clique. **A causa mora no app shell:** o `.layout` tem `overflow-y: hidden` COM conteúdo maior que ele (medido no Chrome: 3067 × 825). O radio escondido é `position: absolute`; sem ancestral posicionado o bloco containing dele é o **bloco inicial**, então ele escapa do contexto de scroll do `#main` — e ao receber foco (todo clique no `label` dá foco) o navegador rola TODOS os ancestrais roláveis para trazê-lo à vista, inclusive o de overflow oculto. Rolar um contêiner `overflow: hidden` empurra o conteúdo para fora **sem barra e sem volta**. **Medido, com controle:** focar o radio jogava `.layout.scrollTop` de 0 para **1696** e o cartão de `top: 464` para `top: -1232`; focar o campo de título ou os botões do MESMO cartão deixava em **0** — ou seja era exclusivo do radio, não um defeito pré-existente do shell. **Conserto: `position: relative` no `.vop-cardstyle` + `top: 0; left: 0` no radio**, que o ancoram no fieldset. O layout não muda (fieldset e espaçamento medidos idênticos). Alternativa medida e igualmente válida: pôr o radio EM FLUXO (`position: static`, tamanho 0) — descartada só porque em fluxo ele consome um `gap` do flex. **Não trocar por `display: none`/`visibility: hidden`:** tiram o radio da ordem de foco e o segmentado perde a navegação por seta e o grupo nomeado que o browser dá de graça.
        - **A guarda de regressão precisou IGNORAR comentários, e isso é a armadilha do próprio teste:** a explicação acima, escrita dentro da regra CSS, contém o texto `position: relative` — então com o comentário no meio a **prosa satisfazia o check** e apagar a declaração passava verde. É a armadilha já registrada neste projeto ("asserir o TEXTO do arquivo reprova a documentação") de cabeça para baixo. A guarda é declaradamente mais fraca que a medição (não há motor de layout nas suítes): ela existe para as duas declarações não se separarem caladas. Quatro sabotagens conferidas numa cópia em temporário, com controle passando — e a primeira tentativa de sabotagem foi INVÁLIDA porque substituía a primeira ocorrência no arquivo, atingindo outra regra: sabotagem tem de ser ancorada na regra certa.
      - **Cosmético consertado junto:** o seletor esticava na largura toda do cartão (`.vop-cand` é grid, e item de grid estica), lendo como faixa em vez de controle. `justify-self: start` → medido 1052px para **218px**, mesma altura.
      - **Verificação FEITA no app real** (Chrome, `index.html` com o helper dublado, 8 cartões): clicando em todos os oito, alternando — deslocamento **0**, rolagem do `.layout` **0**, do `#main` **0**, largura 218 em todos, cada cartão guardando a SUA escolha, zero erro de console. Navegação por teclado preservada (radio focável, grupo nomeado, `label[for]` ligado).
      - **Terceira opção: SEM card (decisão do usuário, 2026-09-02):** `titleCardStyle: 'nenhum'`, rótulo **"Sem card"**, valor do MESMO conjunto fechado — e não uma segunda chave (`titleCardOff: true` ao lado exigiria decidir quem manda quando os dois discordassem). Já existia um jeito de não ter card (apagar o título), mas ele **custa o título**, que também nomeia o arquivo baixado e o cartão da Central; esta opção separa as duas coisas. **Provado no pixel:** o still com título preenchido e `nenhum` é **byte a byte igual** ao still sem título (`754cfd7b…`), e difere das duas marcas.
        - **`titleCardPreset` passa a devolver `null`, e `null` é desfecho LEGÍTIMO — nunca sintoma de valor quebrado**, que já caiu na marca padrão dentro do `titleCardStyleOf` (mesma disciplina do `if override is not None`). **O padrão NUNCA é `nenhum`:** valor torto tem de cair na marca de sempre, não apagar o card calado.
        - **O portão do card é load-bearing:** `comLegenda && medida.texto && cardMarca`. Sem o terceiro termo, "Sem card" chega ao componente como `card={null}` e o `MARCAS[card.marca]` **derruba o render inteiro** — não é um card faltando, é tela preta. A identidade é resolvida numa const (`cardMarca`) usada pelo portão E pela tag: resolver duas vezes deixaria os dois poderem discordar.
        - **A lista `TITLE_CARD_STYLES` traz LITERAIS, não a constante interpolada:** ela é lida como TEXTO por regex pelo `serve.py` e pelo `video-ops.js` (as duas outras cópias, que não podem importar o módulo), e com `TITLE_CARD_SEM` no meio os leitores extraíam o NOME da constante e a paridade acusava divergência falsa. O check 13a2 amarra as duas pontas.
        - **Sabotagens conferidas em cópia temporária, com controle passando:** portão sem o terceiro termo, padrão virando `nenhum`, `nenhum` ganhando identidade no registro, e a constante divergindo da lista — **todas reprovam num check nomeado**. Uma NÃO reprova e está certo assim: trocar o ramo explícito por `TITLE_CARD_PRESETS[chave] || null` é comportamentalmente idêntico para toda entrada de hoje (medido), então nenhuma prova os distingue; quem protege o caso futuro (identidade declarada sem entrada no registro) é o 13a3, que compara os dois tamanhos.
        - **Verificado na tela real** (Chrome, `index.html` com helper dublado): três rótulos na ordem `Primo Rico · Puro Ecommerce · Sem card`, exatamente um marcado por vez alternando entre os três, deslocamento 0, rolagem 0, zero erro de console, e o **título permanece no campo** — que é o ponto da opção existir.
      - Checks: `.\provas.ps1` (`test_serve.py` 222 → **244**, `studio/test-preset.mjs` 257 → **305**, `test-video-ops.js` 49 → **63**, `test-video-ops-dom.js` 86 → **99**). Stills: `cd studio && node preview-titulo.mjs` (casos 12 a 16).

  - **Qualidade do clip para postar: enquadramento escolhido, cor certa, áudio no volume do feed (decisão do usuário, 2026-09-03):** relatado como *"os clips que saem do Estúdio não estão com qualidade boa o suficiente para postar"*. Executa `docs/PLANO-qualidade-clip-tiktok.md`; o contrato de execução, com os números e as medições, está em `docs/CONTRATO-qualidade-clip.md`. **O defeito não era um: eram seis**, e três só apareceram OLHANDO o quadro e o `ffprobe`.
    - **O que estava errado, MEDIDO no arquivo entregue** (`bNkQaTQ4SE0-66-140-editado.mp4`, caminho Remotion): `pix_fmt=yuvj420p`, `color_range=pc` (faixa cheia), `color_space=bt470bg` (BT.601/PAL SD), `color_transfer`/`color_primaries` **unknown**, 3,93 Mbps e **-21,0 LUFS** (~7 dB abaixo do feed). Conferido num segundo clip: os mesmos três tags errados, não é caso isolado. **A causa:** o `npx remotion render` rodava só com `--props`/`--public-dir`/`--log=error`, então valia o default `--image-format=jpeg --jpeg-quality=80` — todo quadro passava por um JPEG (matriz BT.601 em faixa cheia) antes do H.264 — e `--color-space=default`, que emite **lista vazia**: nenhuma tag, nenhuma conversão. Depois: **`yuv420p` · `tv` · `bt709` · `bt709` · `bt709` · -14,0 LUFS**, medido ponta a ponta nos três enquadramentos.
    - **`-color_primaries` e `-color_trc` são IGNORADOS por este FFmpeg — medido, e mudou a receita do plano.** Com as quatro flags de encoder, `color_transfer` e `color_primaries` saem `unknown` (em qualquer ordem, com ou sem `-profile:v high -level 4.0`); só `setparams` no fim da cadeia de filtro grava as quatro, e **também no `h264_qsv`**. Daí `worker.COR_TAGS` viver no FILTRO: é o único ponto do 9:16 que já tem dono único (`build_filter`), então a tag não virou uma quarta cópia de parâmetro de encode — e flag que o build ignora ficaria no código parecendo que faz algo.
    - **Enquadramento: UM campo de conjunto FECHADO, `Inteiro / 1:1 / 4:5`.** `worker.REFRAMES` virou `("blur","crop","crop11","crop45")` e **`serve.PROFILES` DERIVA dele** (`REFRAMES + ("horizontal",)`) — duas listas à mão divergiriam. Validador em cada camada (`serve.reframe_profile`, `preset.reframeOf`, `video-ops.reframeOf`), mesmo padrão do `titleCardStyle`: **ausente ou desconhecido cai em `blur`**, porque todo trecho já salvo tem de sair exatamente como sai hoje. O `crop` de quadro cheio fica **interno** (`REFRAMES_OFERECIDOS` é subconjunto próprio) — ele amplia 1,78× e segue alcançável só editando a query, como sempre foi.
      - **O achado estrutural: `crop11`/`crop45` são o `blur` com a fonte PRÉ-RECORTADA**, não variantes do `crop` — eles têm tarja, o `crop` não. Então é **um** segmento prependido a `[0:v]` (`crop='min(iw,ih*L/A)':'min(ih,iw*A/L)'`) e a cadeia inteira que já existia é reaproveitada. **Um ponto de inserção, não quatro.**
      - **`min` dos dois lados garante a proporção EXATA** seja a fonte mais larga ou mais estreita que ela, e é por isso que a altura é determinística nos perfis de recorte. **Provado com FFmpeg real em quatro fontes** (1920x1080, 1280x720, 1080x1080 e 720x1280 já vertical): `crop11` → 1080x1080 e `crop45` → 1080x1350 em **todas**.
      - **Por que o `crop` NÃO recebe o segmento genérico — medido:** `min(iw, ih*9/16)` num 1920x1080 dá 607,5, o filtro **trunca para 607**, a proporção deixa de ser 9:16 e o `decrease` erra a altura por 2 px → **1080x1918**, que o `check_output` reprova; numa fonte 1280x720 o encode falhou de vez. Ele mantém o ramo dedicado que já tinha. Quem "unificar" isso depois reintroduz o defeito (check 16h).
      - **Armadilha achada no código que os agentes deixaram pela metade:** os ramos 2 e 3 do `_reframe_chain` ignoravam o segmento, então `crop11`/`crop45` **sem miniatura** (todo MP4 local do Passo 3) vazavam para o `return` final e saíam como `crop` de quadro cheio — escolher 1:1 entregava um 9:16 recortado, calado.
    - **`video_box` é dono ÚNICO da altura em TODOS os perfis, e dele saem TRÊS props.** O `render_props` chamava `video_box("blur", media)` **duas vezes** com o rótulo cravado (uma para `legendaBase`, uma para `bandaAltura`); agora é UMA chamada e dela saem `reframe`, `videoAltura`, `legendaBase` e `bandaAltura`. `band_height` e `margem_inferior` **não mudaram uma linha** — já recebiam a altura. É o dividendo do dono único.

      | perfil | altura do vídeo | tarja | `margem_inferior` | região da fonte 1080p | escala |
      |---|---|---|---|---|---|
      | `blur` | 608 | 656 | 705 | 1920x1080 | 0,56× |
      | `crop11` | 1080 | 420 | 506 | 1080x1080 | **1,00× nativo** |
      | `crop45` | 1350 | 285 | 393 | 864x1080 | 1,25× |
      | `crop` (interno) | 1920 | 0 | 269 | 607x1080 | 1,78× |

      - **O 1:1 é pixel a pixel a partir de um 1080p** — mais que dobra a altura do vídeo sem um único pixel interpolado. É isso que o torna o passo natural do meio, e não só "o suave". De uma fonte 720p a conta piora (1,50× e 1,88×), e é para esse caso que a guarda de fonte existe.
      - **A legenda SOBE junto com o vídeo** e nunca encosta na faixa de botões do TikTok: o teto `ZONA_UI_PCT` (1651) só morde no `crop`. Medido: bases em 1215 / 1414 / 1527.
      - **`round(OUT_W * alto / largo)` é obrigatório** — não trocar por `round(OUT_W / (largo/alto))`. Medido: `1080 * 1080/1920` dá exatamente `607.5` → 608; a outra forma cai em `607.4999…` → 607. Um pixel de deriva calada entre props e filtro sai daí. E a altura sai **PAR** do dono único, porque o mesmo número vai ao filtro E aos props e o yuv420p exige par.
    - **Remotion: o `Palco` recorta a FONTE, por função pura.** `preset.palcoGeometria(reframe, altura)` — `blur` devolve **a marcação de hoje** (largura 100%, altura natural), os perfis de recorte devolvem caixa `1080 × altura` com `overflow: hidden` e o vídeo em `cover`. **`videoEscala` fica 1 e `videoCentroPct` fica 0.5** (checks 1d e 6a seguem valendo): isto é recorte da FONTE, **não** a ampliação de 1,45× removida em 2026-08-26. Continua **exatamente uma** tag `<Video>`. Paridade aritmética com o FFmpeg: `cover` num 1080x1350 mantém 1080/2400 = 45%, o mesmo 55% que o filtro corta.
      - **ARMADILHA MEDIDA OLHANDO O FRAME — `objectFit` é PROP, não CSS.** Dentro de `style` o `<Video>` do `@remotion/media` o **ignora**, e o 1:1 saía com o vídeo em 608px de altura dentro da caixa de 1080 (recorte nenhum), com a legenda — ancorada como se o vídeo tivesse 1080 — caindo na tarja. O único aviso era uma linha no console do render: *"Use the `objectFit` prop instead of the `style` prop"*, no meio da saída. Por isso o `palcoGeometria` devolve `objectFit` **separado** do estilo.
      - **`ancoraVideo` NÃO é o `ancoraBanda`, e a diferença é o ZERO:** lá ele é resposta legítima (fonte já vertical não sobra tarja), aqui altura 0 é vídeo nenhum — um `>= 0` entregaria caixa de altura zero, com o fundo inteiro à vista e nenhum erro.
    - **A tarja parou de repetir a manchete.** `THUMB_LUZ`/`fundoLuz` 0,42 → **0,18** e desfoque na MINIATURA (`gblur=sigma=14` no filtro, `blur(28px)` no CSS). Olhando um corte real, as duas tarjas mostravam a miniatura **nítida, com a manchete dela legível DUAS vezes** — não lê como moldura, lê como três imagens empilhadas, duas com título próprio disputando com a legenda e com o card. **Isto NÃO reintroduz o desfoque removido em 2026-08-27:** aquele borrava a *tira central do vídeo* (cuja armadilha era o topo virar teto escuro e a base mesa iluminada); este borra a miniatura, que é uma imagem só e cujo texto legível é o defeito. O escurecimento continua **multiplicativo**, nunca `eq=brightness`.
      - **Os dois números são o MESMO desfoque em unidades diferentes:** o CSS define `blur(r)` como gaussiana de desvio padrão `r/2`, então `blur(28px)` ≡ `gblur=sigma=14`. O check de paridade cobra `fundoDesfoque == 2 * THUMB_DESFOQUE_SIGMA`, **não** igualdade — igualdade simples esconderia um desfoque com o dobro do raio.
      - **ARMADILHA MEDIDA OLHANDO O FRAME — blur em CSS traz TRANSPARÊNCIA de fora da borda.** A tarja saía esmaecendo para o fundo nas bordas (vinheta na borda do quadro e falha clara na fronteira com o vídeo). A geometria do `<Img>` é **inflada** pelo raio (deslocamento negativo + tamanho maior) para as bordas esmaecidas caírem fora do `overflow: hidden`. O FFmpeg não tem esse problema: o `gblur` replica a borda.
    - **Compressão: `-preset slow -crf 18`, e o QSV saiu.** `video_encoder_args()` é chamado **só** pelo `render_cut`, ou seja só no 9:16 — o `horizontal_args` tem args próprios e **nunca** usou QSV. Então "restringir o QSV ao horizontal", como o plano dizia, significaria **acrescentar** encode de hardware a um caminho que o plano manda não tocar; ele simplesmente deixou de ser usado e o probe foi apagado por não ter mais chamador (BP-007). Nesta máquina o probe passava, então **todo** 9:16 do Passo 3 vinha de `h264_qsv -global_quality 22`, que entrega menos qualidade por bit. `-preset slow` custa tempo dentro do `render_lock`, que é único e sem timeout — sabido.
    - **Áudio a -14 LUFS, ponto único no `_send_video`.** `worker.finish_video` (nasceu `normalize_audio`; renomeada em 2026-09-04, ver o bullet das tags de cor) num passe só, `-c:v copy -af loudnorm=I=-14:TP=-1.5:LRA=11 -c:a aac -b:a 192k`, **antes do `_keep`** — é o choke point das duas rotas (`/api/video-cut` e `/api/remotion-render`), a mesma razão pela qual o `_keep` mora lá. Medido no clipe real de 74 s: **-21,0 → -14,3 LUFS em 11 s de parede** (~0,15 s por segundo de clipe, ruído ao lado dos ~11 s/s do Remotion), com a faixa de vídeo byte a byte igual. Passe único de propósito: ele é dinâmico e acerta dentro de ~0,3 dB; dois passes custariam o dobro pela fração.
      - **`-c:v copy` NÃO conserta tag de cor** — medido: depois do passe o clipe antigo continua `pc`/`bt470bg`. É o que confirma que a cor tem de ser resolvida na ORIGEM, e é por isso que ela mora no filtro e nas flags do Remotion, nunca aqui.
      - **Estado sempre visível (BP-008):** `X-Clip-Audio` **sempre presente**, conjunto FECHADO (`worker.AUDIO_STATES` = `normalizado` | `AUDIO_SEM_FAIXA` | `AUDIO_NORM_FAILED`) filtrado por `serve._audio_state`, irmão do `_caption_state` — o valor vai a um cabeçalho HTTP e vira frase na tela, e valor livre nesse trajeto quebra os dois lados. **Falha não derruba o download:** entrega o original e diz que não normalizou. **As DUAS rotas leem** — o caminho Remotion não lia cabeçalho de estado nenhum até aqui.
      - **ARMADILHA MEDIDA no meu próprio código:** `worker.probe` lê o cabeçalho do stderr do FFmpeg e **NÃO levanta** em arquivo ilegível — devolve tudo `None`. Sem a guarda do `videoCodec`, um MP4 truncado saía como `AUDIO_SEM_FAIXA`, colapsando "não tem áudio" com "não consegui ler" — exatamente o par que o conjunto fechado existe para manter distinto (a mesma razão do `CAPTIONS_NOT_AVAILABLE` × `EXTRACTION_FAILED`).
    - **A tela: radio irmão do `Card visual`, em DOIS lugares.** `.vop-reframe` no cartão do trecho do YouTube (`clip.reframe` → `clipFieldWrite` → corpo do `/api/remotion-render`) e na linha do corte do Passo 3 (`cut.reframe` → `cutFieldWrite` → `&profile=`, no lugar do **`'blur'` cravado** em `video-ops.js:2576`). Uma função monta o HTML dos dois — duas cópias divergiriam no primeiro rótulo mudado. Sem re-render (BP-001): o `:checked` nativo desenha o estado, e no cartão do trecho `projectsPersist()` faz a escolha voltar com o projeto.
      - **A armadilha de 2026-09-02 viaja com o molde e é obrigatória:** `position: relative` no fieldset + `top: 0; left: 0` no radio `absolute`. Sem isso o radio escapa do contexto de scroll, o foco rola o `.layout` (que tem `overflow-y: hidden`) e a tela fica numa faixa de ~200px sem barra e sem volta — medido na época: `scrollTop` de 0 para 1696. Mais `justify-self: start`, senão o fieldset estica na largura do cartão e lê como faixa em vez de controle.
      - **Prévia do recorte: o cartão NÃO tem `<video>`** (verificado — a prévia é um `<iframe>` do player do YouTube, montado só no "Prever"). As máscaras vão sobre ele: duas faixas de `21,875%` (1:1) e `27,5%` (4:5) de cada lado, `pointer-events: none` — sem isso o player para de aceitar clique. **Não anima:** é informação, aparece com a escolha e fica parada. E o rótulo do radio diz a porcentagem em TEXTO, para valer com a prévia fechada.
      - **Guarda de fonte sem rota nem cabeçalho novo:** `sourceScale`/`sourceWarning` espelham o `serve.source_scale`, e as dimensões já chegam ao navegador (resposta do `/api/yt-fetch`; `videoWidth`/`videoHeight` no Passo 3). Limiar **1,30×**, que sai da tabela de reamostragem e não de gosto: é onde o `crop45` deixa de ser 1,25× (aceito). **Não bloqueia o render** — informa que aquele trecho sai mole naquele enquadramento (BP-008).
      - **A terceira cópia acompanhou:** `FFMPEG_FILTERS` ganhou `crop11`/`crop45` e o `ffmpegCutCommand` passou a `-preset slow -crf 18` + as tags de cor. Senão o comando manual de emergência mente, e o CLAUDE.md já registra que ele tem de casar com o `build_filter`.
    - **Verificação visual FEITA nos dois renderizadores, nas três proporções.** Remotion: stills 17/18/19 do `preview-titulo.mjs` com fonte sintética 16:9 — confirmado olhando que o vídeo ocupa 608/1080/1350 px, que o `blur` sai com a geometria intocada e a fonte inteira, que o recorte é horizontal, que **não há esmaecido nas bordas nem falha na fronteira vídeo/tarja**, e que a legenda cai DENTRO do vídeo nos três. FFmpeg: as três alturas medidas **no pixel** do arquivo renderizado (checks 16b/16c/16d lêem uma coluna de 1px e contam as linhas que diferem da moldura chapada) — asserção sobre o filtro provaria só que ele SABE montar o crop.
      - **Armadilha do próprio teste:** em yuv420p o `crop` arredonda a largura para múltiplo de 2, então `w=1` virava **0** e o FFmpeg recusava. `format=gray` ANTES do crop resolve — em gray não há subamostragem de croma.
    - **`--jpeg-quality=100` primeiro, PNG não medido.** `RENDER_IMAGE_FORMAT`/`RENDER_JPEG_QUALITY` são o **botão de calibragem** e estão nomeados no topo do `serve.py`. O `--color-space=bt709` já resolveu a cor inteira (ele emite `-color_range tv` **e** `zscale=…range=limited`, ou seja conversão real — lido no `dist/` instalado, não na doc), então o PNG passou a ser só sobre a perda geracional do JPEG. **A comparação de banding JPEG-100 × PNG não foi feita** — se ela levar ao PNG, o `RENDER_SEC_PER_CLIP_SEC` sobe junto, senão volta a armadilha de 2026-08-26 (clipe longo morrendo no meio e a tela mostrando erro depois de 15 min). E **não use `overrideFfmpegCommand`** para isso: o Remotion já emite um `-vf` próprio no modo bt709, e acrescentar outro faz o último vencer e mata o `zscale`.
    - **DUPLA COMPRESSÃO CONFIRMADA no caminho YouTube, e NÃO consertada aqui — é o maior lastro que sobra.** O risco 4 do plano era "a suspeitar"; está medido: `ytclip.fetch_section` roda o yt-dlp com `--download-sections` **+ `--force-keyframes-at-cuts`**, e a ajuda do próprio binário diz *"This is slow due to needing a re-encode"*. Ou seja o trecho chega ao Remotion **já recodificado**, e o render é a segunda geração. O caminho MP4 local não tem intermediário (um encode só), e o `horizontal_args` foi descartado como suspeito (a pasta de clips nunca é lida como `-i`). **Fora do pedido de propósito:** tirar a flag faz o corte começar no keyframe anterior — até um GOP antes do pedido —, o que desloca o conteúdo e **dessincroniza a legenda**, que é rebaseada no início do corte. Trocar precisão de corte por uma geração de encode é decisão nova, não uma das seis frentes aprovadas.
    - **Fora deste pedido, de propósito:** rastreamento de falante (o recorte é **centrado**, sem tracking); legenda (posição, tempo, karaokê, revisão, ênfase); detecção e recomendação de cortes; portão de direitos; card do título; `horizontal_args` (preset, CRF e tags de cor); `cloud/`.
    - **Sabido e NÃO consertado:** (a) o `render_lock` é único e sem timeout, e o `-preset slow` aumenta o tempo dentro dele; (b) o bitrate entregue não foi validado contra o alvo de ~8 Mbps — as medições de saída usaram fonte sintética (`testsrc2`), que não representa o custo de bits de conteúdo real; (c) a fonte baixada continua **não medida** (o cache estava limpo), então o número real da guarda de fonte sai no primeiro download; (d) não há `X-Clip-Background` dizendo qual fundo saiu — o motivo do fallback de miniatura vai só ao console, e continua sendo o único ponto sem estado visível na interface.
    - Checks: `.\provas.ps1` (`test_serve.py` 244 → **276**, `test_worker.py` 92 → **110**, `studio/test-preset.mjs` 305 → **322**, `test-video-ops.js` 63 → **72**, `test-video-ops-dom.js` 99 → **108**). Stills: `cd studio && node preview-titulo.mjs` (casos 17 a 19).
    - **Os três itens que ficaram em aberto foram MEDIDOS com conteúdo real (2026-09-04).** Fonte: `bNkQaTQ4SE0-415-461.mp4` (trecho de podcast já baixado — 1920x1080, 24 fps, **1,71 Mbps**, `yuv420p`/`tv`/`bt709`), 12 s, miniatura = quadro real do próprio vídeo. **`testsrc2` não serve para isto**: ele não tem pele, grão nem gradiente, e é justamente o custo em bits desse conteúdo que estava sob pergunta.
      - **O alvo de ~8 Mbps é INALCANÇÁVEL e desnecessário para este conteúdo — e a razão não é o encoder.** Medido ponta a ponta pelo servidor de verdade: **1.459.284 bps de vídeo / 1.657.797 bps de container**. O plano mandava descer o CRF para 16 se ficasse abaixo de 8; **descer não resolve**, porque a escada inteira mora numa ordem de grandeza abaixo do alvo: com a MESMA composição, CRF 18 = **1,11 Mbps**, CRF 16 = **1,49**, CRF 14 = **2,05**, e o mesmo quadro **sem perda nenhuma** (`-qp 0`) = **25,27 Mbps**. Para encostar em 8 seria preciso CRF ~6, ou seja gastar ~6× mais bytes descrevendo ruído — exatamente o que o pedido proíbe ("não otimize para bater um número se isso custa qualidade").
      - **A prova de que CRF 18 não é o gargalo é a distância até o SEM PERDA, não o bitrate:** contra o `-qp 0`, o CRF 18 entrega **PSNR 53,1 dB / SSIM 0,9980**. Transparência visual começa perto de 45 dB, então já estamos muito acima. CRF 16 compra **+0,9 dB por +34% de bytes** e CRF 14 **+1,8 dB por +85%** — imperceptível, pago em arquivo. **Bitrate baixo aqui é conteúdo, não defeito:** o quadro é um 1080p de 1,7 Mbps REDUZIDO para 1080x608 mais duas tarjas borradas e escurecidas (que a entrega de 2026-09-03 tornou lisas de propósito). Não há 8 Mbps de informação lá dentro. **O `-editado.mp4` antigo era 3,93 Mbps e o novo é 1,46 — isso NÃO é regressão:** os bits de antes pagavam a miniatura NÍTIDA das tarjas, que era o defeito visual removido.
      - **JPEG-100 FICA; PNG foi medido e reprovado no custo.** Isolando o formato do quadro do H.264 (still PNG × still JPEG-100 da MESMA composição): **PSNR 62,2 dB no luma** — perda geracional invisível. **E o PNG não tem vantagem de banding, que era a única razão de considerá-lo:** na tarja escura o PNG tem **34 níveis únicos** e o JPEG-100 **36** (o JPEG tem *mais*, porque o ruído de DCT funciona como dither e SUAVIZA o contorno). Olhando o recorte da tarja amplificado 8×, os anéis de quantização são os mesmos nos dois, e os do PNG são se acaso um pouco mais duros. Custo do PNG no render completo: **140,1 s contra 87,9 s (+59%)**, ou 11,6 × 7,3 s por segundo de clipe. **O banding que existe é do 8-bit sobre a miniatura escurecida** — quem quiser matá-lo um dia mexe em dither/10-bit, não no formato do quadro.
      - **`--color-space=bt709` conserta 3 dos 4 tags no Remotion, não os 4 — e isto CORRIGE o §0.3 do contrato**, que deduziu do `dist/` que as quatro sairiam. Medido no arquivo entregue: `yuv420p` ✔, `color_range=tv` ✔, `color_space=bt709` ✔, mas `color_transfer` e `color_primaries` continuam **unknown**. A causa já estava escrita no §0.1 do próprio contrato e ninguém ligou as duas pontas: **este FFmpeg IGNORA `-color_primaries`/`-color_trc` como flag de encoder**, e é exatamente essa flag que o Remotion emite. O caminho FFmpeg sai com os **quatro** (medido), porque nele a tag vem do `setparams` DENTRO do filtro. **Os dois que causavam dano visível — faixa cheia e matriz 601 — estão consertados nos dois caminhos**, e transfer/primaries ausentes num HD com matriz bt709 são inferidos sem ambiguidade. **CONSERTADO no mesmo dia, pelo PLANO 1** — ver o bullet das quatro tags, abaixo: era o `-bsf:v h264_metadata` mesmo, e o risco ao `loudnorm` morreu com uma guarda de uma linha.
      - **Áudio conferido no ENTREGUE, não no intermediário: `-14,05 LUFS`, `-1,50 dBTP`** (alvo `I=-14:TP=-1.5`), `X-Clip-Audio: normalizado`, aac 48 kHz estéreo 192 kbps, e vídeo/container/áudio com a mesma duração (12,067 / 12,1 / 12,1 s). É o `_send_video` fazendo o passe antes do `_keep`, como desenhado.
      - **Armadilha do próprio teste (não re-descobrir):** o `clipToken` de produção **É o nome do arquivo, com extensão** (`token = os.path.basename(got["path"])`), e o destino do render é `"edit-" + token`. Semear o cache com um token sem `.mp4` faz o Remotion falhar sem saber o formato de saída, e a rota devolve **500 com `"O Remotion falhou: sem detalhe"`** — stderr vazio, mensagem que não aponta a causa. Não é defeito do produto (o servidor sempre gera o token com extensão); é o que confere primeiro quem for escrever fixture de render.
  - **A vez no renderizador passou a ter PRAZO (decisão do usuário, 2026-09-04):** o `render_lock` era único, compartilhado pelas três rotas, com `with` bloqueante e **sem prazo** — já registrado como sabido e não consertado, e o `-preset slow` aumentou o tempo dentro dele. Com o teto proporcional, um clipe de 600 s (o `MAX_CUT_SEC`) segura o lock por ~4 h e um `⬇ Baixar vídeo` do Passo 3 ficava pendurado sem nada na tela dizer por quê. `CutHandler._render_slot()` (contextmanager) troca o `with` cru nos **três** pontos: espera `RENDER_LOCK_WAIT` (300 s, botão de calibragem) e, não alcançando a vez, levanta `WorkerError("render_busy")`.
    - **503, e NÃO o 409 que o CLAUDE.md sugeria.** O 409 já é o combinado do contrato para "a fonte saiu do cache", e o navegador responde a ele **reenviando o arquivo inteiro** (`video-ops.js:2169`): um "estou ocupado" com 409 faria o Passo 3 subir o MP4 de novo para ouvir a mesma recusa. Nenhuma frase nova foi precisa na tela — as duas rotas já passam o corpo do erro pelo `renderError`, que mostra a mensagem do servidor.
    - **As duas grandezas ficam SEPARADAS:** `render_lock_wait` limita quem está na **fila**; o `timeout` do subprocess (`render_budget` / `ffmpeg_timeout`) limita o processo que está **rodando**. Um teto de fila do tamanho do render devolveria a espera de 4 h que este guarda existe para matar.
    - **O `finally` solta em QUALQUER saída** — inclusive exceção do FFmpeg. Sem ele, um render que explode travaria todo render futuro: falha permanente, que é pior que espera longa.
    - Provas comportamentais (bloco 29, servidor real): a requisição VOLTA em vez de pendurar (com `join(30)`, para a regressão reprovar em 30 s em vez de travar a suíte), volta 503/`render_busy`, esperou de fato antes de desistir, a recusa **não vaza** o lock (o download seguinte passa), exceção dentro da vez solta o lock, e o 29i reprova qualquer rota que volte ao `with` cru. **Armadilha do próprio teste:** a docstring do `_render_slot` não pode citar o `with` antigo por extenso — a prosa fazia o 29i reprovar (a armadilha de asserção sobre TEXTO deste projeto, agora ao contrário).

  - **As QUATRO tags de cor no caminho Remotion, e o passe de áudio virou passe de ACABAMENTO (decisão do usuário, 2026-09-04):** executa `docs/PLANO-1-tags-cor-remotion.md` e fecha a pendência (a) do relatório do mesmo dia. O MP4 do `/api/remotion-render` saía com **3 das 4** — `color_transfer` e `color_primaries` em `unknown` — porque este FFmpeg **IGNORA `-color_primaries`/`-color_trc` como flag de ENCODER**, que é exatamente a forma em que o Remotion as emite; o caminho FFmpeg acerta as quatro porque nele a tag vem do `setparams` DENTRO do filtro (`COR_TAGS`). **Gravidade honesta: baixa** — as duas que causavam dano visível (faixa cheia e matriz 601) já estavam consertadas nos dois caminhos. **O que se ganha é a simetria entre os dois renderizadores**, que neste projeto é o defeito.
    - **`worker.COR_BSF` é a MESMA coisa que o `COR_TAGS`, dita em outro lugar: filtro para quem RECODIFICA, bitstream para quem COPIA.** `-bsf:v h264_metadata=colour_primaries=1:transfer_characteristics=1:matrix_coefficients=1:video_full_range_flag=0` reescreve o VUI do SPS, **não** os pixels. Entrou no passe que **já rodava** com `-c:v copy`, então custa **+0 bytes de vídeo e zero I/O novo** — nada de um segundo passe só pela tag. Medido no arquivo produzido: mesma contagem de quadros, mesmo `pix_fmt`, mesmo codec (check `16q`).
    - **`overrideFfmpegCommand` continua PROIBIDO, e é por isso que o `-bsf:v` foi o caminho:** o Remotion já emite um `-vf zscale` próprio no modo `bt709` (conversão REAL de faixa, não só tag), e acrescentar outro `-vf` faz o último vencer e **mata o `zscale`** — perderíamos algo que funciona para ganhar duas tags informativas. `-x264-params colorprim=…` funciona mas só é alcançável por ali, mesma armadilha. **Não criar `studio/remotion.config.ts`.**
    - **A guarda de uma linha é o CORAÇÃO da entrega:** o `h264_metadata` **falha em stream que não é H.264**, e a falha derrubaria o passe inteiro → `AUDIO_NORM_FAILED` → o clipe sairia **sem normalizar o áudio**, que é audível e importa muito mais que duas tags. Então `["-bsf:v", COR_BSF] if info.get("videoCodec") == "h264" else []`: não-H.264 perde só a tag, **nunca** o áudio. É o check `16u`, e sem ele o conserto trocaria um defeito invisível por um audível.
    - **`normalize_audio` → `finish_video`.** Uma função chamada "normaliza áudio" que também escreve tag de cor é a mentira que produz o próximo bug. Três pontos (`worker.py`, `serve.py`, as suítes). **`AUDIO_STATES` e os três valores NÃO foram renomeados** — eles são sobre o ÁUDIO, saem no `X-Clip-Audio` e têm frase do outro lado no `video-ops.js`; mexer neles arrastaria `serve._audio_state` e a tela.
    - **O ramo SEM faixa de áudio passou a produzir arquivo, e sem ele a entrega seria meia.** Ele saía com um `return AUDIO_SEM_FAIXA` antecipado, então o clipe sem áudio **nunca receberia a correção de cor**, calado. Corte de podcast sempre tem áudio, mas o `/api/video-cut` aceita MP4 local qualquer. Um passe só nos dois casos: com áudio `-af LOUDNORM -c:a aac …`, sem áudio `-an`. E o `serve._finish_video` aceita os **dois** estados de sucesso pela mesma razão — filtrar por `AUDIO_OK` apagaria justamente o arquivo do ramo em que o `-bsf:v` foi a única coisa que o passe fez (check `28o4`).
    - **A prova lê o ARQUIVO, e o veredicto é o COLAPSO das três num `bt709` só.** Sem ffprobe (CONTRATO_TRABALHADOR §9 — este projeto não instala outro binário), a leitura é o cabeçalho do próprio FFmpeg, como no `16j`. Medido: `yuv420p(tv, bt709/unknown/unknown, progressive)` → `yuv420p(tv, bt709, progressive)` — **o FFmpeg só imprime uma vez quando primárias, transferência e matriz coincidem**, então o `bt709` único É a assinatura de sucesso.
    - **Armadilha do próprio teste (não re-descobrir):** contar quadros com `re.search(r"frame=\s*(\d+)")` pega uma linha de **PROGRESSO**, não o total — medido, **877 de 1350** nos 45 s da fonte sintética. O check `16q` reprovava com os dois arquivos idênticos. É `findall(...)[-1]`, e o comentário está no código.
    - **Quatro sabotagens exercitadas numa CÓPIA em temporário** (nunca na árvore de trabalho — a lição de 2026-08-26), cada uma reprovando num check nomeado: tirar o `COR_BSF` do comando (`16p`), inverter a guarda de H.264 (`16u`), voltar o `return` antecipado do ramo sem áudio (`16t`) e deixar o `serve._finish_video` só com `AUDIO_OK` (`28o4`). Controle conferido verde na cópia antes de cada uma.
    - **Fora deste plano, de propósito:** o caminho FFmpeg (já saía com as quatro — é conferido pelo `16x`, não tocado), `--color-space=bt709`/`--image-format`/`--jpeg-quality` (medidos e fechados), bitrate/CRF, e o `X-Clip-Background` — que segue sendo o único ponto do 9:16 sem estado visível na interface.

- **Lançamento público, Fase 1 — só a ANÁLISE vai ao ar (decisão do usuário, 2026-08-26):** o prompt original de lançamento (Cloudflare Pages + Supabase Auth + RLS + R2, com o processamento de vídeo desligado) foi reescrito antes de executar: ele entregava uma tela de login onde todo botão dizia "ainda não disponível", porque **o produto é o processamento**. A Fase 1 publica o que já funciona e é legalmente limpo — `probe()` + `candidates()`, que leem metadados públicos, capítulos, legenda e o heatmap, e **não baixam um byte de mídia**. Cortar e renderizar continuam locais, no Estúdio, onde a declaração de autorização já existe. Ver `PASSO-A-PASSO.md` (passos manuais) e `docs/LANCAMENTO-decisoes.md` (o portão da Fase 0).
  - **`cloud/` (novo) NÃO é código morto (BP-007).** `cloud/probe_server.py` é stdlib puro (~200 linhas) e **importa** `ytclip` sem alterar uma linha dele — fronteira única preservada. Expõe **duas** rotas: `POST /probe` e `GET /health`. `ytclip.fetch_section` está no módulo importado e **não é exposta de propósito**; a prova é comportamental (`test_probe_server.py` bloco 3 arma uma bomba em `fetch_section`/`produced_media` que fica ativa durante a requisição HTTP real). `cloud/Dockerfile` = `python:3.12-slim` + yt-dlp **binário** (nunca `import yt_dlp`), **sem FFmpeg** (o probe não chama) e usuário sem privilégio. `.dockerignore` corta o contexto de 1,1 GB para ~150 KB e mantém o `serve.py` **fora da imagem** — o container fica sem ter como servir download, render ou `/clips`, nem por regressão futura.
  - **`web/` (novo) é um SEGUNDO ponto de entrada, não uma reorganização.** Nada foi movido: medido, o `index.html` tem **0 `import`**, **15 `<script src>` cuja ordem importa** e **138 buscas por id de DOM** no script inline — mover arquivo ali é trocar benefício zero por tela em branco calada. O CSS de `web/index.html` é inline com os **valores copiados** do `video-ops.css`/`design-system.css` (o cartão precisa do prefixo `#view-video-ops` lá, que não existe aqui). `ytStepHTML`/`ytCandidateCardHTML` **não são exportados** pelo `video-ops.js`, então o esqueleto do cartão é remontado — mesmas classes, mesmos textos. **O Cloudflare Pages tem que apontar para `web`**: a raiz publicaria o site pessoal inteiro.
  - **A resposta pública é RECORTADA:** `{video, note, candidates[]}` com 8 campos por candidato. Fora de propósito: `cues` (medido: **3.830 falas / 331 KB** num podcast de 2h07 — a resposta cai para **4,2 KB**), `state` (resto do pipeline) e `category` (palpite que nunca aparece na tela). `note` FICA — é o aviso que o detector já escreve e que a tela de hoje já mostra.
  - **A tela NÃO tem portão de direitos nem estado de legenda.** Aqui não existe download: checkbox que não guarda nada é teatro. E as frases de legenda/heatmap pertencem ao **Passo 2** (sidecar de MP4 local); usá-las aqui seria inventar estado. O único vestígio de heatmap no cartão é o chip de sinal.
  - **Armadilhas medidas na revisão adversarial (não re-descobrir):** (1) **`Handler.timeout` não é opcional** — o padrão do stdlib é "sem prazo", e um socket que anuncia `Content-Length` e não manda o corpo prende uma thread para sempre **antes** do Turnstile e do semáforo (Slowloris); (2) **recusa tem que fechar a conexão** — 404/413 respondem sem ler o corpo, e com keep-alive esses bytes viram a requisição seguinte (reproduzido: uma requisição, **duas respostas**); o conserto mora no `_json`, então `do_GET` herda; (3) **o `message` do `ffmpeg_failed` carrega o stderr CRU do yt-dlp** — vai ao log, nunca à resposta; o usuário recebe frase de conjunto fechado (`UPSTREAM`), mesmo princípio do `CAPTION_STATES`; (4) **`MAX_BODY` de 2 KB era menor que o corpo exigido** pelo próprio servidor (token do Turnstile ~2048 chars); (5) **resposta obsoleta repopulava a lista** com trechos de outro vídeo — contador de requisição, e `busy` liberado fora do guarda; (6) **token do Turnstile é de uso único** (`reset()` a cada tentativa) e "script não carregou" ≠ "não resolveu": frases diferentes, senão manda marcar caixa que não existe.
  - **Continua fora, de propósito:** contas, banco, RLS, R2, fila e render na nuvem. Quando o render for para a nuvem, é pelo **caminho FFmpeg** (medido: o Remotion faz ~11 s de render por 1 s de clipe; o FFmpeg entrega o mesmo 9:16 com legenda queimada em segundos) e a imagem ~~**precisa da fonte Inter instalada**~~ **não precisa mais de nada instalado (2026-08-28)**: a Inter viaja no repo e o filtro a recebe por `fontsdir`, então o container só precisa do FFmpeg.
  - **A Prova A não está respondida:** o probe funciona da máquina local (medido: 2h07 → 7,5 s), mas **ninguém testou de um IP de datacenter** — o YouTube pode devolver verificação de robô. É o portão do `PASSO-A-PASSO.md`, Passo 3.
  - Checks: `py -3.12 cloud/test_probe_server.py` (63) · `node web/test-app.js` (28).

- **Shooting Range REMOVIDO (decisão do usuário, 2026-08-17):** o mini-game de mira (adicionado em 2026-08-14, commit `41a2a61`) saiu por inteiro - `shooting-range-core.js`, `shooting-range.js`, os dois testes, a seção `#view-shooting-range`, o `<style>` das classes `.sr-*`, o item do menu, o bloco `.hub-block[data-group="jogos"]` da Central e o export `window.appActivateView`. Não recriar nem referenciar `SRCore`/`shootingRangeSetActive`/`pp_shooting_range_v1`. **A pesquisa do range 3D (`docs/shooting-range/project-audit.md`) e o `vendor/three.min.js` CONTINUAM no repositorio, sem nada ligado** - não são código morto a remover, sao insumo de um plano não iniciado (BP-007).

# Performance (Carregamento e Render) — auditoria 2026-06-30
Otimizações aplicadas (cirúrgicas, sem alterar regra de negócio; ~5.3 MB + 3 requests fora do caminho crítico inicial):
- **Imagens de fundo recomprimidas** (Pillow q80, dimensões preservadas, originais com backup): `assets/capa-bg.jpg` 2.0MB→187KB · `assets/capa-sidebar.jpg` 2.5MB→211KB · `assets/themes/brasil/{bg,sidebar}.jpg` reduzidas. Total dos fundos **5.4MB→0.7MB (−88%)**.
- **`xlsx-populate.min.js` (642KB) virou lazy-load:** removido o `<script>` síncrono; helper `ensureXlsx()` no topo do script inline injeta a lib sob demanda; `await ensureXlsx()` antes dos 3 usos (`fatLoadFile`, `fatWriteRecords`, `fatWriteGroupedRecords`). Só carrega ao usar o Faturador.
- **CSS `transition: all` (16×) → propriedades específicas** (`background-color, border-color, color, box-shadow, transform, opacity`), preservando `var(--ease)`/`var(--ease-spring)` (cumpre a regra Emil, §5).
- **`supabase-*.js` retirados do carregamento (backend PAUSADO):** os 3 `<script>` ficam comentados em `index.html` (~L2935) com a receita de reativação inline; **arquivos mantidos no repo**.
- **Mantido de propósito:** `backdrop-filter`/`blur` (visual "vidro premium"); arquivos órfãos **não carregados** (`Copa.js`, `CAPA-DE-FUNDO.png`, `copa-score.js`, `wizards-score.js`, `fba-shipments.js`) — removê-los não muda o load. **Reorg 2026-07-14:** os `.js` órfãos foram movidos p/ `arquivo/` e os docs (`ARQUITETURA.md`, `CONVERSAS.md`, `PROCESSO-FBA-ENVIOS.md`, `resumo-chat-cyberlab.md`) p/ `docs/`. Nada carregado/agendado saiu da raiz — site e radares intactos.

# Economia de Tokens e Contexto (Think in Code)
- Pare de agir como um processador de dados e atue estritamente como um gerador de código.
- NUNCA leia arquivos grandes para o contexto. Em vez disso, use ferramentas de sandbox (como bash ou `ctx_execute`) para criar e rodar scripts que analisem os dados e façam o output (`console.log`) apenas do resultado final.

# Diretrizes de Comportamento (Karpathy)
Siga rigorosamente estes princípios ao escrever código:

## 1. Pense antes de codar
- Não faça suposições e não esconda sua confusão.
- Declare suas premissas explicitamente. Se o pedido for ambíguo, pergunte ao usuário em vez de adivinhar.

## 2. Simplicidade primeiro
- Escreva o mínimo de código que resolva o problema. Nada de código especulativo.
- Se 200 linhas puderem ser resolvidas com 50 linhas, reescreva.

## 3. Mudanças Cirúrgicas
- Toque apenas no que for obrigatório para a tarefa.
- Limpe apenas a sua própria bagunça. Nunca tente "melhorar" ou refatorar códigos vizinhos que já estão funcionando.

## 4. Execução por Metas
- Transforme a instrução imperativa em um objetivo verificável com testes.
- Planeje brevemente, execute e verifique se funcionou antes de dar o resultado final como concluído.

## Bug Prevention Rules

### BP-001: Nunca usar `dblclick` em elementos cujo handler de `click` chama `render()`
- **Causa:** O primeiro clique dispara `render()`, que substitui o `innerHTML` do contêiner. O segundo clique cai sobre um novo nó DOM — o browser só dispara `dblclick` se ambos os cliques atingirem o **mesmo** elemento. O `dblclick` nunca dispara.
- **Regra:** Para ações de edição inline em listas dinâmicas, use sempre um botão explícito (ex: ✎) verificado no handler de `click` **antes** do handler que chama `render()`.

### BP-002: Nunca usar SheetJS (community) quando o requisito for preservar a planilha original
- **Causa:** A edição community do SheetJS não grava estilos, validações de dados nem formatação (são recursos pagos). Um round-trip `XLSX.read` → `XLSX.write` reconstrói o arquivo do zero e descarta esses elementos.
- **Regra:** Para editar um `.xlsx` preservando a estrutura original (estilos, fórmulas, validações), use `xlsx-populate` (edita o XML in-place) e altere **apenas** as células necessárias com `.value()`. Nunca reconstrua a planilha inteira.

### BP-003: Toda dedução embutida num valor monetário deve ser exibida no resultado
- **Causa:** Na Precificação, o frete de R$ 5 era aplicado corretamente no cálculo, mas não aparecia no resultado dos modos "Bater concorrente" e "Ajuste %" — o usuário reportou como bug ("não está colocando a taxa"). Dedução invisível é indistinguível de dedução ausente.
- **Regra:** Sempre que um valor exibido embute taxas/frete/descontos, o resultado deve listar explicitamente cada dedução aplicada (ex: "frete de R$ 5,00 embutido"). Nunca mostrar apenas o número final. **Corolário (BUG-003):** toda dedução **exibida** (ex: linha "Impostos" com DIFAL/PIS) deve também ser **aplicada** ao número que ela compõe (a M.C.). Exibir uma dedução sem subtraí-la é tão errado quanto subtrair sem exibir.

### BP-004: Toda divisão por um valor vindo de input do usuário deve guardar o denominador zero
- **Causa (BUG-004):** Na sub-aba Multi-plataforma, `mcPct = mc / valorReceber` com venda 0 produzia `-Infinity`, exibido como "-Infinity%". `pct()`/`brl()` não tratavam valores não-finitos.
- **Regra:** Antes de dividir por um campo editável, guarde o zero: `denominador ? num / denominador : 0`. Funções de formatação (`pct`, `brl`) devem tratar `NaN`/`Infinity` retornando 0.

### BP-005: Não carregar bibliotecas pesadas (>100KB) de forma síncrona no caminho crítico
- **Causa:** `xlsx-populate.min.js` (642KB) era um `<script>` síncrono no meio do `<body>`, bloqueando o parser e baixado em **toda** visita, embora só seja usado quando o usuário processa a planilha do Faturador.
- **Regra:** Bibliotecas grandes usadas apenas sob interação devem ser carregadas **sob demanda** (injeção de `<script>` via helper que retorna `Promise`, ex.: `ensureXlsx()`), com `await` antes do primeiro uso. Nunca síncrono no carregamento inicial.

### BP-006: Imagens devem ser comprimidas antes do commit (alvo prático <250KB)
- **Causa:** `capa-bg.jpg` (2.0MB) e `capa-sidebar.jpg` (2.5MB) eram fundos decorativos absurdamente sub-comprimidos (uma imagem de ~1900px deveria pesar ~200KB) — ~4.6MB no caminho crítico só de fundo.
- **Regra:** Imagens decorativas/de fundo: recomprimir (qualidade ~80, dimensão = tamanho de exibição). Nunca commitar JPG/PNG multi-MB. Fazer **backup do original** antes de recomprimir.

### BP-007: Antes de remover "código morto", confirmar que a UI ativa não o referencia
- **Causa:** O `CLAUDE.md` afirmava que o módulo Inventário fora removido, mas `amazon-inventory.js` continuava vivo e load-bearing (`#view-amazon-inventory`, roteamento em `setView`). Remover o `<script>` como "morto" teria quebrado a tela — pego em teste e revertido.
- **Regra:** "Código morto" só é morto se nada ativo o referencia. Antes de remover `<script>`/arquivo: verificar (1) se a UI/roteamento ativo o usa e (2) se algum global que ele expõe é consumido. **A documentação pode estar desatualizada — a fonte da verdade é o código.**

### BP-008: Toda automação de UI deve ter estado visível em TODOS os casos — inclusive quando não age
- **Causa (BUG-005):** O autofill de CEST por NCM funcionava, mas era mudo em 3 de 4 estados: NCM ambíguo deixava as opções num `<datalist>` invisível até clicar no campo, NCM desconhecido e planilha sem índice não diziam nada. O usuário reportou como "não está aparecendo a opção de preencher" — automação silenciosa é indistinguível de automação quebrada (irmão do BP-003).
- **Regra:** Recurso automático (autofill, sugestão, detecção) deve comunicar visivelmente o que fez **e o que não fez**: preencheu → confirmação; tem opções → opções à vista (chips/botões, nunca só `datalist`); não achou → dizer por quê. Nenhum ramo termina sem feedback.

### BP-009: Ao gravar em planilha de terceiros, espelhar o TIPO e o estilo das linhas de dados existentes
- **Causa (BUG-006):** O Faturador gravava NCM/CEST como texto (valor cru do `<input>`), mas o template da Amazon guarda essas colunas como número — o Excel marcava a célula em vermelho ("Validação de formato") mesmo com o valor correto. E a linha inserida, por nascer abaixo da faixa formatada do template, perdia o estilo (ex.: custo sem `numberFormat`).
- **Regra:** Valor certo com tipo errado é dado errado para quem valida. Antes de gravar numa planilha alheia: (1) inspecionar como as linhas existentes guardam o dado (`t=s` texto vs número) e gravar igual; (2) linha nova copia o estilo por coluna de uma linha de dados real (`cell.style(props)` do xlsx-populate). Exceção: dígitos com zero à esquerda ficam texto (número perderia o zero).

### BP-010: Formatação/validação de XLSX também vive no extLst (x14) — auditar lá antes de concluir "não há regra"
- **Causa (BUG-007):** CF clássica, estilos (xf com herança/tema) e sharedStrings estavam limpos, mas o Excel pintava o CEST de vermelho. A regra morava em `extLst > ext > x14:conditionalFormattings` (com dxf inline), onde o Excel guarda CF moderna (ex.: fórmulas entre abas) — e o mesmo vale para `x14:dataValidations`. Agravante: templates de terceiros com faixas espatifadas por inserção de linha podem aplicar a regra de UMA coluna em OUTRA (a lista de "Atividade" caiu na coluna CEST).
- **Regra:** Ao investigar "por que a célula está com essa cara", a varredura deve cobrir TODAS as camadas: xf da célula (com herança xfId e cor de tema), CF clássica (avaliando o deslocamento relativo da âncora), **CF x14 no extLst**, DV clássica e **DV x14**, formato numérico com cor (`[Red]`) e rich-text do sharedStrings. E ao corrigir template alheio: remover/limpar SÓ o fragmento comprovadamente quebrado, preservando as regras legítimas (cirúrgico, idempotente, com try/catch que nunca derruba a gravação).

### BP-011: Arquivo de dados gerado/editado por agente headless deve passar por `node --check` antes de concluir
- **Causa (BUG-008):** `curso-expandir.ps1` (claude.exe -p headless) reescreve `curso-data.js`; uma execução cortou no meio da última aula e deixou JS inválido. `curso.js` tem guarda `if (!window.CURSO) return` e falhou em silêncio → aba CyberLab em branco. Arquivo de dados truncado é indistinguível de válido até o parser rodar.
- **Regra:** Toda automação (PowerShell/headless) que gera ou edita `.js`/dados deve validar sintaxe (`node --check <arquivo>`, e para globais um `node -e` que confirme o global) ANTES de dar a tarefa por concluída — e, quando não puder validar dentro do agente, uma guarda no runner deve registrar FALHA alta no log em vez de shipar o arquivo quebrado calado. A defesa primária é não gravar arquivo inválido; a guarda no consumidor (`window.CURSO`) é a última linha, não a primeira.

### BP-012: CSS injetado via `var CSS = \`…\`` deve ser atribuído ANTES de chamar o injetor (hoisting)
- **Causa (BUG-009):** Em `pratica.js`, `ensureCss()` (`style.textContent = CSS`) era chamado antes da linha `var CSS = \`…\``. A declaração `var` sobe por hoisting, mas a **atribuição** não — então `CSS` valia `undefined` na injeção e o `<style>` saía vazio. `node --check` passa (sintaxe ok); a aba renderiza crua só em runtime. Mesma armadilha existia na `cursos.js`.
- **Regra:** Em renderers que injetam CSS por `<style>`, defina o `var CSS`/const **acima** da chamada que o consome (ou chame o injetor no fim do IIFE, depois da atribuição). Verifique com simulação headless de DOM (stub de `document`/`localStorage`) que o `textContent` injetado é não-vazio — `node --check` não pega erro de ordem de runtime. Preferir CSS estático no `index.html` (como `curso.js`) quando não houver motivo pra injetar.

### BP-013: Re-render que troca `innerHTML` de um container rolável deve preservar a posição de rolagem
- **Causa (BUG-010):** Na aba Fluxos, excluir um card chamava `render()`, que reconstrói o `innerHTML` de `#fluxos-root` e recria o `.fx-canvas-wrap`. O zoom sobrevivia (var de módulo), mas `scrollLeft`/`scrollTop` nasciam em 0 → o canvas saltava para o começo do Fluxo, perdendo a região que o usuário olhava.
- **Regra:** Antes de um re-render que substitui o `innerHTML` de (ou acima de) um elemento com `overflow:auto`, capture `scrollLeft`/`scrollTop` do container rolável e restaure-os depois (helper como `renderKeepingScroll()` em `fluxos.js`). Vale para toda mutação que re-renderiza e não muda a extensão rolável (excluir/editar/duplicar). Exceção legítima: quando o conteúdo muda de contexto (trocar de quadro/board), resetar o scroll é o comportamento certo — aí NÃO preservar.

### BP-014: Identificador não declarado num ramo dependente de DADOS passa por `node --check` e pela suíte inteira
- **Causa (BUG-011):** `video-ops.js` usava `MAX_CANDIDATES` — nome que só existia em `video-worker/ytclip.py` — dentro de `Array.isArray(p.candidates) ? p.candidates.slice(0, MAX_CANDIDATES) : []`. Sintaxe válida, então `node --check` passou. O ternário curto-circuita, então quem nunca tinha analisado um vídeo jamais avaliava a referência e via o site normal. Quem tinha um projeto salvo com `candidates` perdia **o site inteiro**: `projectsSanitize` lança → `projectsLoad` lança → `init` morre no `DOMContentLoaded` → `#video-ops-root` fica vazio → **tela preta**, com nada além do erro no console. Relatado como "por que ele está ficando só com tela preta", e irreproduzível em perfil limpo — o que manda investigar o servidor, o cache e o CSS antes de olhar para o lugar certo. As 124 verificações do Estúdio passavam porque `projectsSanitize` **não era exportada**: nenhuma delas a chamava.
- **Regra:** Função que sanitiza, migra ou carrega dado PERSISTIDO (`localStorage`, sidecar, JSON salvo) tem de ser **exportada e chamada pelo teste com o dado construído**, e nos DOIS ramos — campo presente e campo ausente. Cobrir só o ramo vazio é cobrir o caminho de quem nunca usou a ferramenta, e o ramo que quebra é justamente o do usuário antigo. `node --check` prova sintaxe, não fiação (mesma raiz do "`in arquivo` só prova que alguém escreveu a palavra"), e "abriu na minha máquina" prova o perfil limpo, não o do usuário. **Corolário:** constante espelhada entre Python e JS (aqui, o teto de sugestões) precisa de check que compare os dois números — divergirem calados faz um lado descartar dado que o outro mandou.

---

## 5. Micro-interações e Polimento (Emil Kowalski)
- **SEMPRE invoque a skill `emil-design-eng` (Skill tool) ANTES de qualquer trabalho de UI/CSS/design** neste projeto — sem o usuário precisar pedir — e aplique o framework dela.
- Foque na percepção de velocidade do usuário e atrito zero.
- Use física baseada em molas (spring animations) em vez de transições lineares duras de CSS.
- Garanta a "interrompibilidade": se o usuário cancelar um clique ou gesto no meio, a animação deve reverter com fluidez natural.
- Princípios não-negociáveis: easing custom forte (nunca `ease-in` em UI), durações <300ms, `:active { transform: scale(.97) }` em tudo clicável, `transition` com propriedades específicas (nunca `transition: all`).
- **Sem "cara de IA":** proibido glow neon gratuito (`0 0 Npx` colorido), gradiente arco-íris e qualquer efeito sem função. Profundidade = sombra realista (offset + contida). Beleza é alavanca; detalhes invisíveis somam.
